# Secret Wars Volume 1 — Implementation Progress

Started: 2026-06-22
Status: Phases 0–4 COMPLETE (2026-06-27). All effects built + audited; Phase-4 fix pass done. Remaining before merge: `sandbox-review` (separate session) + Paul playtest (both modes), then coordinator merges.
Branch: `secret-wars-vol1` (worktree)

## Scope (locked — staged assets only)

**IN:** 14 heroes (56 cards); 4 villain groups (Domain of Apocalypse, Limbo, Manhattan Earth-1610, Sentinel Territories); 2 masterminds (Madelyne Pryor, Nimrod) + their 8 tactics; 8 schemes; **3 henchmen groups (M.O.D.O.K.s, Thor Corps, Ghost Racers — Fight effect only; ratified in-scope 2026-06-25)**; Banker bystander; Sidekicks (reuse existing stack).

**DEFERRED (out — do NOT build):** Wasteland group, The Deadlands (zombie) group, **the Ghost Racers Ambush "Rise of the Living Dead"** (the Ghost Racers henchman Fight effect IS built — only its Deadlands Ambush is deferred), all 30 Ambitions + Ambition Row / "A Player is the Mastermind" mode, Wasteland Hulk + Zombie Green Goblin masterminds. → **Bribe** has zero in-scope cards and is NOT implemented this build; **Rise of the Living Dead** is deferred (Ghost Racers carries no `ambushEffect` → enters the city silently like a vanilla henchman, verified).

---

## Phase 0: Pre-flight — ✅ Complete
- Inventory finalized (Pass 2, in-scope clean): `docs/card-inventory/final/secret-wars-vol1.md`
- Mechanics ref ready: `docs/expansion-mechanics/secret-wars-vol1.md`
- Rules notes cached: `docs/rules-notes/secret-wars-vol1.md`
- ⚠️ **Staging images blocker:** `expansions/secret-wars-vol1/` is gitignored + lives only in the main folder, absent from this worktree. Must be copied into the worktree before Phase 1a image import.

## Phase 1: Card Data + Images — ✅ Complete (2026-06-22)
- **Images:** 95 webp copied to production (56 heroes → `Visual Assets/Heroes/Secret Wars Vol. 1/`; 16 villains; 10 masterminds; 8 schemes; 2 henchmen; 1 bystander).
- **cardDatabase.js:** 14 heroes (56 cards, groups 57–70, inner ids 225–280); 4 villain groups (24–27, ids 281–296); 2 masterminds (19, 20) + 8 tactics (297–304); 8 schemes (37–44); 2 henchmen (9, 10); Banker bystander ×3 under `"Secret Wars Vol. 1"` key in `expansionBystanders`.
- Verified: `node --check` clean; all 80 inner ids unique; deferred content absent; in-scope counts match inventory.
- New teams introduced: `"Illuminati"`, `"Cabal"` (need setup-screen filter entries in Phase 2).
- **(d) dual-class:** RESOLVED safe — zero `classes[0]` reads in codebase; all consumers use `.includes()`/`.some()`/iteration; icon renderer already does `classes.slice(0,3)`. Stored dual-class as 2-element arrays (e.g. `["Covert","Tech"]`); color = first class's color (color is display + Grey-check only).
- **Open/flag:** masterminds have NO `epic{}` block (inventory captured no Epic-side data) — confirm whether Epic variants are in scope before 3d. Scheme `endGame`/`twistEffect` are camelCase placeholders to wire in 3e. `backingTrack: ""` (falsy-safe, default music).

## Phase 2: Setup Screen — ✅ Complete (2026-06-22)
- **Scheme scope applied (coordinator decision):** removed Fragmented Realities (id 41, skipped) + Smash Two Dimensions Together (id 44, deferred) from cardDatabase.js + deleted their 2 images. 6 schemes remain (Build an Army of Annihilation, Corrupt the Next Generation, Crush Them With My Bare Hands, Dark Alliance, Master of Tyrants, Pan-Dimensional Plague).
  - Build an Army of Annihilation: at 3e use **M.O.D.O.K.s as the "Annihilation Wave Henchmen" stand-in** (named group is a pre-release printing error; rules-as-intended — scheme spawns its own 10-henchman army from M.O.D.O.K. art/stats).
- **index.html registration:** OVERALL SET + heroes-section Sets filter get "Secret Wars Vol. 1" (matches Revelations' partial pattern — per-section scheme/MM/villain/henchmen Sets filters were never given Revelations either; left as-is). Card entries: 6 schemes, 2 masterminds, 4 villain groups, 2 henchmen, 14 heroes (each behind `<hr data-set="Revelations">`).
- **Team filters:** added `Cabal` + `Illuminati` to #heroteamfilter.
- **expansionSecretWarsVol1.js** skeleton created; script tag wired into the load chain (after Revelations, before updatesContent); added to `sw.js` FILES_TO_CACHE (CACHE_NAME stays `legendary-v5` — bump at merge).
- **Epic toggle:** SW masterminds have no `epic{}` → `script.js:973` (`if (mastermind.epic)`) auto-hides the toggle. No toggle exposed. ✓
- **Verified:** `node --check` clean (skeleton + cardDatabase); grep confirms 6/2/4/2/14 registrations, both team filters, 0 epic blocks, removed schemes absent.
- **ASSET GAP (flag):** no `Illuminati.svg` / `Cabal.svg` team icons exist (staging shipped none) — using `Unaffiliated.svg` placeholder for those 8 heroes + 2 team filters. Reversible; needs proper icons before ship.
- **LIVE SMOKE TEST — ✅ PASS (`/game-test`, 2026-06-23):** game loads with no real console errors (only known sw.js 404); all dropdowns populate (14/4/2/6/2 + Cabal/Illuminati filters); Epic toggle stays hidden for Madelyne; full game started (Madelyne + Pan-Dimensional Plague + Limbo + M.O.D.O.K.s + 3 SWV1 heroes) — **12 SWV1 board images all loaded (naturalWidth>0), 0 broken**; M.O.D.O.K.s/Madelyne/Limbo render with full art. Setup validation correctly reads scheme `requiredHeroes:3` / What-If `requiredVillains:1`. Screenshot: `docs/playwright-runs/2026-06-23/swv1-phase2-smoke-board.jpeg`.
- **Minor observation (benign):** empty `backingTrack:""` on SWV1 schemes emits a one-time `console.warn` "No scheme or backingTrack found, using default" (`script.js:19100`) and plays default music — working as designed (falsy-safe). Optional polish: assign an existing track name per scheme.

### (c) gainSidekick() helper — proposed design (read-only; for Phase 3a-2 build)
Cost-free, cap-free Sidekick gain is genuinely missing (`recruitSidekick()` @ script.js:17782 always sets `sidekickRecruited` + deducts cost). Proposed:
```
async function gainSidekick(destination = "discard") { // "discard" | "hand" | "deckTop"
  if (sidekickDeck.length === 0) { onscreenConsole.log("No Sidekicks remain in the deck."); return null; }
  const sk = sidekickDeck.pop();
  if (destination === "hand") playerHand.push(sk);
  else if (destination === "deckTop") { playerDeck.push(sk); sk.revealed = true; }
  else playerDiscardPile.push(sk);
  onscreenConsole.log(`Gained a Sidekick (<span class="console-highlights">${sk.name}</span>) → ${destination}.`);
  updateGameBoard();
  return sk;
}
```
Does NOT touch `sidekickRecruited` or recruit points (per insert: gain ≠ recruit). Consumers: Magik Rally, King of Wakanda (×3, deckTop via Illuminati), Maximus Enslave, Namor Lead, Ultimate Spidey Marvel Team-Up; also Corrupt-the-Next-Gen "gain to deck top". Mirrors `recruitSidekick()` destination branches minus the cap/cost.
<!-- index.html dropdowns + create expansionSecretWarsVol1.js skeleton + script tag + sw.js FILES_TO_CACHE entry -->

## Phase 2.5: Behavioral Specs frozen — ✅ Complete / FROZEN (2026-06-23)
- `docs/expansion-specs/secret-wars-vol1.md` — ~87 card blocks, **36 LOW-confidence markers** (30 cards + keystone/shared-mechanics blocks; each LOW = mandatory dynamic `/game-test` in Phase 3). Committed BEFORE any Phase 3 code (the Phase-4 blind-compare contract).
- Front-loaded: **Open Questions for ratification** (Q1 "each other player" solo rule + per-card calls; Q2–Q7 scheme/card reading calls) + **Shared Mechanics & Keystone** (Multiple Masterminds engine spec, gainSidekick w/ empty-stack guard, Teleport, Cross-Dim Rampage, dual-class, no-rules-text predicate, gain-as-hero converter, free-defeat, Banker reserve, ≥6-recruit gate, Demon Goblins).
- Authoring: front section + keystone authored by lead; 4 per-card drafts via parallel subagents (inventory + mechanics-doc inputs), every block reviewed against the inventory before freeze.
- **HOLD Phase 3** pending coordinator review of the frozen spec + ratification of Open Questions Q1–Q7. None block the keystone (3a-1); Q1–Q7 gate specific consumers in 3b–3e.

## Phase 3: Effects — 🟨 In progress
- 3a-1 **KEYSTONE — Multiple Masterminds engine** (core change, infrastructure-first): ✅ built (commit `7e58d01`) + coordinator-reviewed (PASS) + **dual-mode `/game-test` PASS 2026-06-23** (see below).
- 3a-2 Other keywords & helpers: 🟨 `gainSidekick()` helper built (expansionSecretWarsVol1.js) — cost-free/cap-free, empty-stack guard, 3 destinations; validator + code-review clean. **`crossDimensionalRampage()` shared helper built (2026-06-23, commit `2275efe`)** — reuse-adaptation of `revealClassOrWound` (name-substring family predicate + Victory Pile in scanned pool); consumed by Apocalyptic Weapon X now, Ultimate Thor + Wolverine of Future Past later. Remaining 3a-2 keywords (Teleport grant, no-rules-text predicate, etc.) still ⬜.
- 3b Heroes (14): ✅ **COMPLETE (2026-06-25).** Backbone + surface 6f Black Bolt + mid-risk batch + Fight the Future + **Untouchable (10a) now done** = all 14 heroes / 56 cards built and gated. pattern-reuse-scout run (rule 9) → full family-grouped build map at **`docs/expansion-progress/secret-wars-vol1-3b-heroes-reusemap.md`** (READ FIRST before building 3b). 56 cards: ~40 pure REUSE, ~3 ADAPT (combined-pool KO helper, revealTopDrawIfCost, Untouchable), **6 BUILD-NEW engine surfaces** (MM free-defeat tactic-pop, Fight-the-Future deck-top fight target, space-keyed defeat listener [Enslave + Stalk], recruit-this-turn tracker [Loner], HQ per-slot cost reduction [Infiltrate HQ], `noRulesText` DB flag [Black Bolt]). **2 scout corrections:** `KO1To4FromDiscard` is discard-only/no-return (NOT a hand+discard pool); there is NO Mastermind free-defeat path / 4-defeat counter (it's tactic-based → tactic-pop). **DONE in this session:** Finding-G back-fix — Ultimate Cap America rewired to reuse Core `CaptainAmericaCountUniqueColorsAndAddAttack` (commit `164039e`), bespoke counter removed, spec assertion updated. **Owed game-test pair ✅ PASS (2026-06-24, state-injection):** (1) Ultimate Cap counter — distinct Hero colors across HAND+played+artifacts, self included (unique-color self card counted), "None" color excluded → delta 5/5 both totals, err null; (2) No More Heroes — clean→+5/+5, S.H.I.E.L.D.-in-hand→0, HYDRA-played→0, err null. Console clean (only sw.js 404 + benign `/undefined` image 404s from fake cards). LOW set needing mandatory game-test: Untouchable, Destructive Whisper, Silence is Golden, Stalk, Fight the Future, Inhuman Mastery, Imperius Rex, No More Heroes, Utter Annihilation.
  - **BUILD-NEW TRANCHE PROGRESS (worker, 2026-06-24):**
    - **Surface 6f `noRulesText` predicate + Black Bolt ×4 ✅ DONE (commit pending).** Hybrid `cardHasNoRulesText()` — explicit DB flag `noRulesText:true` (Speak No Words id230, Inspiration Through Power id265) honored first; runtime fallback (no ability/conditional/keyword/bonus) catches vanilla no-text cards game-wide so Black Bolt fires meaningfully. Keyword check fixes the naive ability-only false-positive. Cards: Destructive Whisper (LOW; reveal-from-hand ≥4→+1 Atk, SPEC-Q4), Silence is Golden (LOW; re-grant a played no-text card's Recruit+Attack, both totals), Hypersonic Scream (draw per no-text played), Speak No Words (flag-only trivial). **Gate:** dynamic `/game-test` PASS (state-injection: predicate real-flags+runtime+3 negatives; Whisper 4→+1/3→0; Scream 3→drew3; Silence valid→+1A/+2R + no-op path) · expansion-validator 7/7 0 issues · `/code-review` (high, 2 finder angles) no surviving findings (async-await-at-dispatch candidate REFUTED — engine awaits async unconditional abilities via promise chain @ script.js:11411-11431, + empirical game-test) + comment-accuracy nit fixed. No `gameMode` branching → mode-agnostic, no dual-mode divergence.
    - **DESIGN NOTE for coordinator — predicate scope (spec left open):** went hybrid (explicit flag + game-wide runtime fallback) rather than tagging every vanilla card. Rules-faithful (a no-text card IS any card with no rules text; vanilla S.H.I.E.L.D. basics qualify) and robust. Reversible if a narrower scope is wanted.
    - **MID-RISK BATCH ✅ DONE (worker, 2026-06-25; commits `b47fdcc` build + `1f36f62` review fixes).** Loner (10d), Infiltrate HQ (10e), Stalk + Enslave + henchman-defeat counter (10c), Inhuman Mastery (no-op special + [CABAL] counter consumer), MM free-defeat tactic-pop (surface 6) + Imperius Rex (either/or) + Utter Annihilation. **Engine surfaces:** 6 new per-turn flags in script.js (`heroRecruitedThisTurn`, `lonerAttackApplied`, `maximusEnslaveActive`, `blackPantherStalkActive`, `henchmenDefeatedThisTurn`, `infiltrateHQDiscountedCards`) reset in endTurn + initGame; Enslave paid in `defeatBonuses` (cardAbilities.js, `!isMastermindDefeat` gate); Stalk + henchman tally in the 3 post-defeat handlers; Infiltrate per-card cost discount read in `showHeroRecruitButton`; Loner claw-back in `recruitHeroConfirmed`. **`freeDefeatMastermind`** reuses the engine's full normal-defeat tail (createMastermindCopy → collectMastermindRescueOperations → handleMastermindPostDefeat); pops ONE Tactic per free defeat (= one of the 4 defeats), tactics===0 guard blocks Golden Final-Showdown auto-deliver. **Imperius Rex either/or** folded into the unconditional ability (DB `conditionalAbility` set `None`) so the "Instead" replacement can't double-fire. **Gate:** expansion-validator **7/7, 0 blocking/0 advisory**; `/code-review` (high, 3 finder angles) → 3 findings fixed (Loner Hero-type gate + floor; freeDefeatMastermind engine-reuse), 2 refuted/accepted. **Dynamic `/game-test` ✅ PASS (state-injection, dual-mode where mode-sensitive):** Loner (+2/clawback/recruit-first/Bystander-no-clawback/floor); Enslave (2 free defeats → 2 Sidekicks, cap untouched); Stalk (Streets/Rooftops prompt, Bridge none); Inhuman Mastery (henchman counts, regular doesn't, no-op special, [CABAL] +N); Imperius Rex (not-met→villain picker, met→either/or→MM tactic-pop 4→3); Utter Annihilation (KO-6, threshold, attack<thresh eligibility, MM dispatch tactic-pop); Infiltrate HQ **both modes** (golden rotate-to-rightmost / whatif in-place, discount through affordability gate); freeDefeatMastermind **both modes** (golden+whatif tactic-pop). Console clean (only benign sw.js + `/undefined` fake-card 404s).
      - **DECISION #3 still open for coordinator (rules):** in GOLDEN, a free Mastermind defeat removes ONE Tactic and deliberately does NOT deliver the strength+4 Final Showdown; if Tactics already 0 it's a graceful no-op (re-prompts the Showdown, never a free win). Faithful per rules-notes Q2 (one fight = one defeat); the Golden Final-Showdown interaction is the app-specific nuance. Reversible.
    - **FIGHT THE FUTURE (10b) ✅ DONE (worker, 2026-06-25; commits `a9cac2a` build + `6926195` review fixes). Option A confirmed by coordinator.** Highest-risk surface (fight a villain on TOP of the Villain Deck — no prior fight-target precedent). **Reuse win:** a paid-fight clone of the SHIPPED `punisherHailOfBulletsDefeat()` (cardAbilitiesDarkCity.js:3131) — peek `villainDeck` top; if `type==="Villain"` (incl. henchmen, which carry type "Villain") grant +2 Attack (both totals), then a `showHeroAbilityMayPopup` offers to fight it at its Attack cost. Accept → spend Attack → run `fightEffect` (with `promptNegateFightEffectWithMrFantastic`) → `villainDeck.pop()` → `defeatNonPlacedVillain` (already SWV1-aware: VP push skips skrulled/gainAsHero, fires defeatBonuses, tallies Henchman for Inhuman Mastery). Decline/unaffordable/non-villain → LEFT on deck, drawn normally next villain phase (no double-resolve). Affordability + payment mirror the engine non-city gate (`getAvailableAttackForFight`/`payLocationAttackCost`) minus the reserve term (a deck card has none). **No `gameMode` branching → mode-agnostic.** No `createVillainCopy` whitelist needed (operates on the live card). **Gate:** expansion-validator **7/7, 0 blocking/0 advisory**; `/code-review` (high, 3 finder angles + verify) → 3 fixes applied (NaN-cost guard, Bribe support, dropped `revealed`-leak), 3 refuted (usesRecruitToFight position-only/false off-grid, Burrow re-entry = faithful base behavior, +2 unconditional = spec-correct). **Dynamic `/game-test` ✅ PASS — dual-mode (golden + whatif):** accept (deck −1, in VP, fightEffect ran, +2/−cost both totals), non-villain (no +2, no popup, left on deck), decline (+2 kept, left on deck, not in VP), unaffordable (+2 granted, no popup, left on deck), Bribe affordability (recruit pays → fight offered, was wrongly refused pre-fix), NaN guard (forced `recalculateVillainAttack`→NaN → falls back to printed Attack, total stays finite). Console clean (only benign sw.js 404). Screenshot `docs/playwright-runs/2026-06-25/swv1-3b-fight-the-future-dualmode.jpeg`.
    - **UNTOUCHABLE (10a) ✅ DONE (worker, 2026-06-25; commits `bc9d500` build + `63c8e4f` review fix). COMPLETES 3b.** Reactive from-hand discard to CANCEL a Villain/Mastermind Fight effect, then draw 3 (Apocalyptic Kitty Pryde, Rare). **Reuse win:** the SAME engine primitive as Mr. Fantastic's Ultimate Nullifier (`promptNegateFightEffectWithMrFantastic`) — a from-hand reactive fight-effect cancel already wired into every fight path; new `offerUntouchableCancel()`/`askToDiscardUntouchable()` in expansionSecretWarsVol1.js modeled on shipped Hellcat `offerSecondChanceReaction` (expansionRevelations.js). Hooked in exactly the 2 in-scope paths: `collectDefeatOperations()` (all villain/henchman defeats) and `resolveTacticEffects()` (mastermind tactics), each offered only when Mr. Fantastic did NOT already negate. **Out of scope (card text = "Villain or Mastermind"):** Location own-fights + each-other-player triggers (script.js negate sites) and the Burrow keyword — deliberately NOT hooked. Cancels the Fight effect only; defeat/VP push unaffected (caller-side, same as the MrF negate). Draw-3 via `drawCard()` (not a point grant → no cumulative-total pairing). No `gameMode` branching; no `createVillainCopy` whitelist (reads only `villainCopy.name`). **Review fix (`63c8e4f`):** decoupled the MM-tactic hook from the FF-loaded guard — the offer was nested inside `if (typeof promptNegateFightEffectWithMrFantastic === "function" && !Mysterio)`, so it would skip Untouchable for MM tactics if Fantastic Four were absent (asymmetric with the FF-independent villain hook). Now gated on the Mysterio exclusion only, with the MrF prompt typeof-guarded internally. Behaviorally inert in this build (FF always loads before SWV1 in index.html) but removes the coupling. **Gate:** expansion-validator **7/7, 0 blocking/0 advisory** (createVillainCopy N/A confirmed); `/code-review` (high, correctness/timing + cross-file/reuse finders + verify) → 1 finding fixed (FF-guard asymmetry above), 2 dispositioned (Mysterio coupling = flagged below; `cardsInHandThisTurn` side-effect = standard draw behavior, REFUTED). **Dynamic `/game-test` ✅ PASS — dual-mode (golden + whatif), 4 scenarios × 2 modes + no-op guard:** villain ACCEPT (prompt appears labeled, fightRan 0, Untouchable→discard, deck −3, villain in VP, city cleared); villain DECLINE (fightRan 1, Untouchable stays, no draw, villain in VP); MM-tactic ACCEPT (tactic fight skipped, Untouchable→discard, drew 3); MM-tactic DECLINE (tactic fight runs through the early-resolve structure, Untouchable stays, no draw); no-Untouchable-in-hand → no prompt + fight runs. Console clean (only benign sw.js 404). State-injection via real `defeatVillain(idx,true)` + `resolveTacticEffects(tactic)`.
      - **FLAG for coordinator (rules, minor, reversible) — Untouchable vs the Mysterio Mastermind Tactic:** Untouchable inherits Mr. Fantastic's exclusion of the converted "Mysterio Mastermind Tactic" (the Master-Strike→tactic edge case), so it cannot cancel that specific tactic's Fight effect. Conservative match to MrF; if you want Untouchable to cancel ANY Mastermind Fight effect incl. that one, lift the exclusion for Untouchable (one-line). Not blocking.
      - **FLAG for coordinator (base-code edge; NOT fixed in shared code):** `updateVillainAttackValues(card, -1)` reads `cityPermBuff[-1]` (undefined) for an off-grid card; under the **"Portals to the Dark Dimension"** scheme its branch assigns that straight into `attackFromScheme` → NaN. Fight the Future is the first non-city caller of `recalculateVillainAttack`, so it newly exposes this. Guarded LOCALLY in the card (fall back to printed Attack) per the Finding-A/D base-branch discipline; the deeper one-line fix (`currentPermBuff` → 0 when `i < 0` in `updateVillainAttackValues`, script.js:~10459) hardens all future off-grid callers and is a base-branch candidate. Low frequency (one scheme), now non-corrupting on this branch.
- 3c Villains: 🟨 **Domain of Apocalypse group ✅ DONE (2026-06-23, commit `2275efe`).** Apocalyptic Magneto (keystone consumer — `apocalypticMagnetoFight` reuses `recruitXMen`; `apocalypticMagnetoEscape` reuses keystone `ascendToMastermind` + base `magnetoStrike`) + Apocalyptic Blink (reveal→Draw/Teleport), Apocalyptic Rogue (Fight: gain Hero-deck top to discard; Escape: reveal-class→force-discard matching hand card via `showCardSelectionPopup`/`checkDiscardForInvulnerability`), Apocalyptic Weapon X (Fight: `FightKOHeroYouHave`; Escape: `crossDimensionalRampage` Wolverine family). All HIGH-confidence → gate = validator + code-review (no mandatory game-test). **expansion-validator: 0 blocking (7/7 Golden rules); code-review (3 angles: validator + correctness line-scan + spec-conformance/reuse): CLEAN, no findings.** **Limbo group ✅ DONE (2026-06-23, commit `6ef0cea`).** Inferno Colossus (Ambush: MM captures Bystander via `attachBystanderToMastermindFromVillainDeck`; Fight: `FightKOHeroYouHave`), Inferno Cyclops (Ambush: captures onto itself, mirror `demogoblinAmbush`; Escape **[LOW]**: redirects carried bystanders out of `escapedVillainsDeck` onto the main MM — `bystander` whitelist already covers the copy), Inferno Darkchilde (Fight: reveal deck top → KO/Teleport; Escape: Teleport a random hand card), Inferno Nightcrawler (Fight: up to 2 Recruit-icon hand cards gain Teleport via Azazel pattern + `showCardSelectionPopup` loop). **Gate:** expansion-validator 0 blocking (7/7); **Cyclops escape `/game-test` PASS** (bystanders pulled from escapedVillainsDeck → MM +2, Cyclops cleared, Cyclops still escaped); code-review (3 angles) clean except Finding D below. Multi-MM "the Mastermind" picker uses main MM (deferred to 3e per keystone). **Validator's "dead-code splice" claim REJECTED** — verified false at script.js:6143-6150 (`handleVillainEscape` DOES push bystanders to escapedVillainsDeck) + game-test proved the splice fires; removing it would cause double-existence. **Manhattan/Earth-1610 group ✅ DONE (2026-06-23, commit `18b24a4`, amended).** New `gainAsHero` engine flag (dedicated, NOT skrulled-reuse — skrulled misfires under the Skrull Shapeshifters scheme: every Manhattan villain would wrongly gain cost+2 attack) guards the 3 VP-skip defeat handlers (`handlePostDefeat`, `handleHQPostDefeat`, `defeatNonPlacedVillain`) + whitelisted in `createVillainCopy`. Shared `gainVillainAsHero(copy, heroForm)` converter (generalizes `gainScarletWitchAsHero`). 4 fight converters: Cap America (unconditional per-color +Atk), Cap Marvel (Teleport keyword + 2 Recruit), Thor ([RANGED]+3 via reused base `ThorBonusAttack`+`bonusAttack:3`), Wasp ([COVERT]+2 via new `ultimateWaspBonusAttack`); Ultimate Thor escape = `crossDimensionalRampage(["Thor"])`. **Gate:** expansion-validator 0 blocking/0 advisory (7/7 Golden); **dual-mode `/game-test` PASS** (golden+whatif): all 4 VP-skipped→discard with correct Hero form; Cap America +3/+1 per distinct color (both attack totals); Thor [RANGED]+3 & Wasp [COVERT]+2 (both totals); Thor escape both branches (forced Wound on no-match, reveal-avoids-Wound on match). `/code-review` (high, 3 finder angles + verify): no on-branch fix required — see Findings E/F. **Sentinel Territories group ✅ DONE (2026-06-23, commits `35df0e7` + fix `b6eada9`).** 4 deferred "alters/changes the future" next-turn effects via 4 new engine flags + paired turn-start consumers (pattern-reuse-scout survey first — corrected the brief: NO pre-existing Colossus flag existed): Colossus (skip 1 villain card next turn via `applySentinelVillainDrawSkip` in both `drawVillainCard` branches — Golden 2→1, What If? 1→0, stacks), Kate Pryde (+1 Recruit now + +1 next turn paid in `endTurn`, both totals), Rachel Summers (Escape +1 / Fight -1 to ALL villains+MM via a global delta fed through `attackFromScheme` in both attack-setter twins + `recalculateMastermindAttack`; next-turn -1 promoted in `endTurn`), Wolverine (Fight +2 draw next turn via REUSED `nextTurnsDraw`; Escape `crossDimensionalRampage` Wolverine family). **Gate:** expansion-validator 0 blocking/0 advisory (7/7 Golden); **dual-mode `/game-test` PASS** — flag setters, attack-delta through city+HQ+MM pipeline (±1, clamp), endTurn recruit payout + delta promote, Colossus skip integration both modes, Wolverine escape both branches; `/code-review` (high) Findings 1+2 (overlay didn't show delta) FIXED + Finding 4 (cross-game leak) FIXED, re-verified. **3c Villains COMPLETE (all 4 groups).** Next: 3b Heroes (14).
- 3d Masterminds: ✅ **COMPLETE (worker, 2026-06-25).** Nimrod, Super Sentinel ✅ DONE (commits `fee21ec` build + `80b8f90` review fixes + `8451d92` Scatter empty-deck guard). **Madelyne Pryor, Goblin Queen ✅ DONE (commit `8ec2097`).**
    - **Madelyne (Demon Goblins / fight-gate / Master Strike / 4 tactics).** BIG REUSE: the Demon-Goblin mechanic ALREADY EXISTS as the Dark City `demonGoblinDeck` system — NOT the `captureBystanderFromVPToLocation`/Korvac/`placeLocation` path the frozen spec's Shared-Mechanics block + passive entry cited (those are rescue-on-carrier-defeat plumbing; WRONG for this build). Spec corrected with ⚠️ BUILD CORRECTION notes (both blocks) so the Phase-4 blind-compare won't false-flag (mirrors the Nimrod 4-gate spec note).
      - **demonGoblinDeck** (global fightable pile; render/affordability/click + count overlay all gated ONLY on `demonGoblinDeck.length > 0`, init unconditionally via `initializeDemonGoblinDeck()` → works standalone in a Madelyne game, no Dark City scheme needed). **`rescueDemonGoblin()` / `showDemonGoblinAttackButton()` / `#demon-goblin-deck` button** = the existing off-grid fight UI (2 Attack → pop goblin → Bystander to VP + `defeatBonuses`+`bystanderBonuses`+`rescueBystanderAbility`, e.g. Banker rider). **NO new fight UI built.**
      - **Fight-lock:** cloned Nimrod's `isMastermindRecruitLocked()` → **`isMastermindDemonGoblinLocked()`** (returns `demonGoblinDeck.length>0`, FLAG-GATED on DB `unfightableWhileDemonGoblins:true` so it locks Madelyne ONLY). Wired into the SAME 4 mastermind surfaces (`handleMastermindClick` early-return + lock message + both `canAttackMastermind` twins + `canAttackMastermindNZ`). Player clears the lock via the existing #demon-goblin-deck button.
      - **ONE capture helper `madelyneCapture(n)`** → generalized the existing `BystanderstToDemonGoblins(requested=5)` (default preserves the Dark City "Transform Citizens Into Demons" twist caller, which dispatches it with NO arg via `handleSchemeTwist`). ALL Madelyne capture effects feed this single path → captured bystanders ARE Demon Goblins (one store; no `mastermind.bystanders` for her). Cap at Bystander Stack size; `n<=0` graceful no-op.
      - **`madelynePryorStrike`** (Master Strike): `setTimeout(0)` defer (same showPopup-callback hang reason as Nimrod) → snapshot `demonGoblinDeck.length>0` BEFORE capturing 4 → if had goblins, active player `drawWound()` ("each player"=self, spec Q1).
      - **4 tactics:** City of Demon Goblins → `madelyneCapture(5)`; Corrupted Clone of Jean Grey → Q1 SELF-APPLY via local `madelyneRevealXMenOrWound` (adapts `revealClassOrWound` with a `team==="X-Men"` predicate + X-Men.svg icon); Everyone's a Demon on the Inside → Q1 NO-OP (announce-and-skip, names OTHER players' piles); Gather the Harvest → count `team:"Limbo"` villains in `city` + `escapedVillainsDeck` → `madelyneCapture(count)`. No `gameMode` branch → mode-agnostic.
      - **Gate (rule 7):** expansion-validator **0 issues (7/7 Golden rules)** · `/code-review` (high) + independent cold-read reviewer (`superpowers:code-reviewer`) → **no correctness bugs** (the one serious candidate — generalized `BystanderstToDemonGoblins` arg regression — REFUTED: the Dark City twist routes through the no-arg `handleSchemeTwist`, not the `villainCard.plutonium`-gated `handlePlutoniumSchemeTwist`). **Dual-mode `/game-test` ✅ PASS (golden + whatif, state-injection in live games):** capture→goblins land in demonGoblinDeck (all type Bystander, stack decremented); lock ON (isLocked + not attackable @ 30 Atk + handleMastermindClick refusal message) / OFF toggle (empty→attackable, re-add→relock); rescue→Bystander to VP + Attack −2 each; Master Strike snapshot-before ordering (0 goblins→capture 4 / 0 wound; had goblins→+4 / exactly 1 wound); all 4 tactics (City→5; Corrupted Clone wound-branch [no X-Men→+1 wound] + reveal-branch [X-Men→0 wound, "revealed" msg] BOTH modes; Everyone's→0 added + no-op announce; Gather→3 from 2 city + 1 escape Limbo, decoys excluded); alwaysLeads Limbo enforced (Limbo in villain deck, proper setup); Banker rider (Bank HQ reserve 0→2 on a Banker captured-then-rescued). Console clean (only benign sw.js + synthetic-card 404s).
      - **✅ B16 BASE BUG — FIXED ON BRANCH (coordinator-approved, 2026-06-25; commit `657eedb`).** `demonGoblinDeck` was never reset between games — declared once (`let demonGoblinDeck = []` @ script.js:925), only ever push/pop'd, NO reset in the per-game init. A demon-goblin game (Dark City "Transform Citizens Into Demons" OR a Madelyne game) ending with goblins in the pile would leak them into the next game started without a page reload. Pre-existing (also inflated the Dark City scheme's villain attack @ the `demonGoblinDeck.length` reads), but Madelyne newly surfaced it as a HARD fight-lock — a fresh Madelyne game would start spuriously unfightable from stale goblins (→ potentially unwinnable). **Fix:** one line `demonGoblinDeck.length = 0;` added to the SWV1 cross-game-state-bleed reset block at the TOP of `initGame()` (script.js ~4977) — the PER-GAME init path that re-runs every new game (alongside the existing sentinel / Phase-3b flag resets), NOT the once-only `initializeDemonGoblinDeck()` (whose `demonGoblinDeckInitialized` guard gates only the one-time DOM listener attach, correctly left once-only; the listener is kept, only the array is cleared). Per the B7/B12 precedent (base bug crashing/breaking a NEW SW card, only non-dup fix in shared init). Catalogued master-side as **B16**. **Gate:** `node --check` clean; **live `/game-test` PASS** — seed 2 stale goblins on the setup screen → start a new Madelyne game via the real begin-game→initGame flow (NO page reload) → `demonGoblinDeck === 0` (reset fired) AND Madelyne fightable (lock OFF, attackable @ 30 Atk); **regression:** mid-game `BystanderstToDemonGoblins()` push still works (5 pushed / stack −5), gate re-engages on push, rescue/pop works (→4) — confirms the init-only reset doesn't disturb mid-game push/pop and the Dark City flow/attack-inflation path now starts from 0 each game.
    - **Recruit-gate** (`unfightableUnlessRecruit:6` DB flag): NOT `usesRecruitToFight` (Nimrod keeps its Attack cost; the recruit total is an UNLOCK). Reused the `cumulativeRecruitPoints >= N` read (Lady Thor / `ThorHighRecruitReward` pattern) — total GENERATED this turn, so spending after reaching 6 doesn't re-lock. Gated via a single `isMastermindRecruitLocked()` helper (reads `getSelectedMastermind()`) wired into the **4 mastermind affordability surfaces**: `handleMastermindClick` (early-return, no attack button + lock message) + the 3 highlight surfaces (`canAttackMastermind` ×2 in the `updateHighlights()` twins + `canAttackMastermindNZ` in `updateHighlightsNegativeZone`). **Scout correction:** the frozen spec's "3-gate (showAttackButton + 2 updateHighlights)" guidance was written against the city-villain `usesRecruitToFight` precedent and is WRONG for a Mastermind — Nimrod is fought via the mastermind path (`handleMastermindClick` → `showMastermindAttackButton`), so the gates are the 4 mastermind surfaces above, not the city-villain twins. Built to spec INTENT; surfaced the engine-line discrepancy (did not silently rewrite the spec). Secondary-mastermind path (`confirmSecondaryMastermindAttack`) deliberately ungated — Nimrod is always the MAIN mastermind in-scope (flag for 3e Dark Alliance if a random 2nd mastermind could be Nimrod).
    - **Effects (`expansionSecretWarsVol1.js` PHASE 3d section):** shared `chooseRecruitOrAttackIcon()` + `nimrodCardHasIcon()` + `nimrodIconHTML()` (the "Choose Recruit or Attack → act on that icon" pattern built ONCE, reused across the Master Strike + all 4 tactics). `nimrodStrike` (Master Strike — reveal [TECH] Hero to avoid, else choose icon + discard ALL hand cards with it; reuses `revealClassOrWound` control flow + `checkDiscardForInvulnerability`). 4 tactics: `nimrodAdaptAndDestroy` (choose icon → discard ONE chosen card via `showCardSelectionPopup`), `nimrodDetectMutation` (reveal-draw chain off own deck top via `drawCard`), `nimrodScatterTheMutants` (HQ heroes with icon → `heroDeck.unshift` bottom + `refillHQSlot`), `nimrodTeleportAndIncarcerate` (reveal Hero Deck top → if icon, gain + Teleport via `cardsToBeDrawnNextTurn`/`nextTurnsDraw` queue, set-aside-to-next-hand per rules-notes 6a — NOT teleport() which is hand-scoped). Solo handling: Master Strike + Adapt-and-Destroy self-apply per spec Q1 / What If? p.24. Epic toggle confirmed hidden (no `epic{}` block). alwaysLeads = Sentinel Territories.
    - **Gate:** expansion-validator **0 blocking / 0 advisory** (7/7 Golden rules). `/code-review` (high, 3 finder angles + verify) → **2 confirmed findings fixed**: (1) `nimrodStrike` Master Strike popup HANG — it runs inside `showPopup`'s confirm callback (`confirmCallback()` then synchronous `closePopup()` on the shared `.info-or-choice-popup`), so opening its prompt before yielding had it closed out → added a `setTimeout(0)` defer at the top (mirrors `LokiRevealStrengthOrWound`); the 4 tactics are unaffected (their dispatch closes the tactic popup before running the effect). (2) `nimrodScatterTheMutants` empty-deck redraw — reordered `refillHQSlot` BEFORE `heroDeck.unshift`. 3 other findings dispositioned no-change (reveal-pool copy-paste = established codebase pattern; missing playSFX = matches teleport(); inline reshuffle dup = justified peek-before-draw). **Dynamic `/game-test` ✅ PASS — dual-mode (golden + What If?), state-injection in a live game (Nimrod forced via radio so `getSelectedMastermind()` returns it):** recruit-gate (locked <6 / unlocked ≥6 / still-unlocked-after-spend / no attack button + lock message / non-Nimrod never locked); Master Strike (Recruit branch via REAL `handleMasterStrikeEffect` dispatch — proves the defer fix; Attack branch incl. dual-icon card; Tech-reveal avoids penalty); Adapt and Destroy (single discard via picker); Detect Mutation (2-draw chain, non-match left on top); Scatter the Mutants (golden rotation vs What If? fill-in-place — divergence verified; + empty-deck edge BOTH modes); Teleport and Incarcerate (match → gain+queue+nextTurnsDraw; no-match → stays on top). Console clean (only benign sw.js + `/undefined` synthetic-card 404s).
    - **✅ B12 BASE BUG — FIXED ON BRANCH (central guard, 2026-06-25).** `refillHQSlot`'s What If? branch (script.js:5254) called `showHeroDeckEmptyPopup()` — **a function never defined anywhere** (also dead-referenced at expansionPaintTheTownRed.js:1734 / Carrion HQ-KO) → `ReferenceError` on EVERY What If? HQ refill with an empty Hero Deck (M.O.D.O.K.s/Ghost Racers HQ-KO, Infiltrate HQ, Nimrod Scatter, base refills). Per the B7 precedent (base bug crashing NEW SW cards, only non-dup fix in shared code) the coordinator authorized the minimal central guard: `if (!newCard && typeof showHeroDeckEmptyPopup === "function")` at script.js:5254 (slot already correctly left empty above — only the missing notification crashed) + the same one-line guard at expansionPaintTheTownRed.js:1734. Hardens ALL callers. Catalogued master-side as **B12**. Nimrod's Scatter `try/catch` (commit `8451d92`) is now redundant belt-and-suspenders — kept per coordinator.
    - **✅ 3e WATCH-ITEM RESOLVED (secondary-mastermind recruit-gate) — Dark Alliance chunk, commit `354d314`.** `isMastermindRecruitLocked(mm = getSelectedMastermind())` generalized to take an optional Mastermind arg; wired into BOTH secondary-fight entry points (`handleSecondaryMastermindClick` + `handleSecondaryMastermindClick`/`confirmSecondaryMastermindAttack`). `addSecondaryMastermind` now carries `unfightableUnlessRecruit`. **Inert for the shipped Core pool** (none carry the flag — Nimrod is excluded), but future-proof if the pool is widened. **`/game-test` PASS:** locked at cumulativeRecruit 3<6 (fight refused, tactic intact) / unlocked at 6≥6 (fight proceeds) / unflagged 2nd MM never locked.
- 3e Schemes (8; Dark Alliance → keystone consumer; 3 gated on open decisions): 🟨 **Dark Alliance ✅ DONE (2026-06-26, commits `21502e2` build + `354d314` review-fixes + `087e41d` slot-render fix).** Multiple-Masterminds keystone's hardest consumer. **darkAllianceTwist** (expansionSecretWarsVol1.js): T1 random 2nd MM (Core pool via opt-in DB flag `darkAllianceEligible` on the 5 Core MMs — Dr. Doom/Loki/Red Skull/Magneto/Mephisto; excludes main; extensible) +1 Tactic; T2-4 accrue Tactics (1→4, capped); T5-6 EACH MM (main + 2nd) captures a Bystander from the Bystander Stack (Q-B literal "each"); T7 Evil Wins (`endGameConditions` `darkAllianceTwist7` case + `updateEvilWinsTracker` `Dark Alliance` twin). **Keystone carryovers all resolved:** C2 spend floor-clamp; C3 full-MM terminal card VP 0 (VP on Tactics); recruit-gate generalization (watch-item above); single-slot bystander overlay; `secondaryMasterminds` per-game reset in initGame. **GAP-K resolved:** `defeatSecondaryMastermind` async — resolves each cleared Tactic's Fight effect via `resolveTacticEffects` + rescues captured Bystanders via reused `collectMastermindRescueOperations`. **Finding C resolved** (ascension keeps captured Bystanders — transfer in `ascendToMastermind`, push + carried-away-discard-penalty both suppressed for ascending villains in `handleVillainEscape`/`handleVillainEscapeActions`). **Slot-render bug fixed** (`updateSecondaryMastermindSlot` was trapped in updateGameBoard's per-city-cell loop → 2nd MM invisible on an empty city; now called unconditionally). Rulings: rules-notes BATCH 7. **Gate (rule 7):** expansion-validator **0 blocking / 2 advisory** (7/7 Golden); `/code-review` (high, 3 finder angles + verify) → 4 fixes applied (off-by-one extra-fight [2 finders], Finding-C discard penalty, duplicate log, slot-render bug surfaced by game-test), 1 refuted (two-active-secondaries "unwinnable" — slot re-renders to next undefeated, defeatable sequentially); **dual-mode `/game-test` ✅ PASS (golden + whatif, state-injection):** T1 (2nd MM excludes main, 1 Tactic, slot visible on empty city) · T2-4 (1→4 capped) · T5-6 (each MM captures, stack −2/twist) · defeat (exactly 4 fights for 4 Tactics — off-by-one fix; Tactic VPs 5×4 scored; Bystanders rescued; terminal VP 0) · win-gate (2nd MM defeated + main alive → win BLOCKED) · T7 Evil Wins (defeat popup + "N/7 Twists" tracker) · Finding C (bystander rides onto ascended MM, not escapedVillainsDeck) · recruit-gate (lock/unlock/inert). Console clean (only sw.js 404). Screenshots `docs/playwright-runs/2026-06-26/swv1-3e-dark-alliance-{golden-t7-defeat,whatif}.jpeg`. **Remaining 3e schemes: 5 (Build an Army of Annihilation, Corrupt the Next Generation, Crush Them With My Bare Hands, Master of Tyrants, Pan-Dimensional Plague).** ⬜
    - **3e chunk 2a ✅ DONE (2026-06-26, commit `6fb005d`): Crush Them With My Bare Hands + Pan-Dimensional Plague.** Rulings cached rules-notes BATCH 8 ①/②.
      - **Crush Them:** DB `requiredVillains:2`+`extraVillainGroups:1` (Golden 3 / What If? 2 groups via `getEffectiveSetupRequirements`, mirrors Revelations Earthquake precedent). `crushThemWithMyBareHandsTwist` fires `handleMasterStrike` (one `type:"Master Strike"` card → koPile per event; secondary MMs fire abilities but add no koPile card, so one event = 1). `endGameConditions` `crush8MasterStrikes` (koPile master-strike filter ≥8) + `updateEvilWinsTracker` "Crush Them With My Bare Hands" twin "N/8" — counts twist-driven AND natural villain-deck strikes.
      - **Pan-Dimensional Plague:** per-HQ-slot `hqWound[]` (script.js, reset in initGame); wound-token DOM per HQ cell + render loop + `.hq-wound-token` CSS. `panDimensionalPlagueTwist` KOs HQ-adjacent wounds → KO pile (NOT stack), then seeds 1 Wound from `woundDeck` next to each HQ Hero (drains stack). `resolvePlagueWoundOnRecruit` (gain→discard, or pay-1→stack via `canAffordRecruitCost`/`spendRecruitCost`, Negative-Zone-safe) hooked into `showHeroRecruitButton` before refill. `plagueWoundStackOut` (woundDeck empty) + tracker "N Wounds left". Default 30-card Wound Stack (no scheme override).
      - **Gate (rule 7):** expansion-validator **0 issues** (7/7 Golden, twin parity); `/code-review` (high, 3 finder angles + verify) → 1 fix applied (pay-1 → canonical recruit helpers), 3 reported-not-fixed (see WATCH below); **dual-mode `/game-test` ✅**: Crush golden full (setup +1 group, twist→koPile→tracker 0/8→2/8, forced 8 → defeat popup) + setup count BOTH modes (3/2 live); Plague golden full (seed/KO-to-pile/PAY/GAIN/auto-gain/deplete→Evil Wins) + What If? (twist drain + PAY branch). Crush What If? twist = code-equivalence (zero `gameMode` branch) + golden-live; its only divergence (group count) live-tested both modes.
      - **✅ ROTATION-CASE VERIFIED (`/game-test`, GOLDEN, 2026-06-26; chunk 2a close-out):** confirmed the slot-keyed `hqWound[]` survives a Golden Solo HQ rotation. Real game (Grim Reaper + Plague + 5 heroes), `goldenFirstRound=false`, wounds injected on slot 0 (the hero that rotates OFF) + slot 2, then the production `goldenHQRotate()` ran. **Result:** wound count in HQ 5→5 (unchanged); the wounded slot-0 hero (Drax) rotated off the left WITHOUT carrying its wound out (no leak to discard/koPile — `woundLeakedToDiscard/KoPile` both false); tagged wounds stayed at their exact slots through rotation; Wound Stack 23→23 (untouched by rotation). Then the **next `panDimensionalPlagueTwist()` after rotation worked cleanly:** KO'd all 5 HQ wounds → koPile (1→6, NOT back to stack), reseeded 1 per HQ Hero from the stack (23→18), zero errors, tokens render. Console clean (only sw.js 404). Confirms slot-keyed (not hero-bonded) is correct — rationale + DO-NOT-revert recorded rules-notes BATCH 8 ②. Screenshot `docs/playwright-runs/2026-06-26/swv1-plague-rotation-board.jpeg`.
      - **⚠️ KNOWN LIMITATION (Pan-Dimensional Plague) — SHIP AS-IS (coordinator call, fix-effort gate):** the recruit-instant wound gain/pay-1 choice fires ONLY via `showHeroRecruitButton` (the HQ recruit path). Ability-driven free-recruit-from-HQ paths (`recruitHeroConfirmed` direct callers: cardAbilities.js:~11111/~12905/~15798, expansionPaintTheTownRed.js:~4463) skip the choice — the wound stays slot-keyed and is KO'd next twist (no orphaned state; player just misses the optional choice on those rare ability recruits). Central async fix logged master-side to the base-engine backlog. **Documented here so the Phase-4 blind-compare does not flag it.**
      - **🔭 PHASE-4 WATCH-ITEM:** re-run Crush Them's twist→master-strike path LIVE in What If? at the Phase-4 dual-mode gate (backstop, not a gap — this turn it was code-equivalence + golden-live; the +1-group divergence was already live both modes).
    - **3e chunk 2b ✅ DONE (2026-06-26, commit `a447964`): Corrupt the Next Generation of Heroes + the SHARED inject-as-villain helper.** Rulings cached rules-notes BATCH 8 ③.
      - **Shared helper `stampCardsAsInDeckVillains(sourceCards, stamp)`** (script.js, before generateVillainDeck): generalises the two inline seeds (Secret Invasion/Skrull + House of M) — type:"Villain", base attack 0 (live Attack via attackFromScheme), skrulled:true (post-defeat handlers skip VP push), originalAttack/originalType captured, + per-scheme stamp. **SHARED INFRA — also serves ⑤ Master of Tyrants (2d) for its 12 tactics-as-villains.** The two existing inline seeds left un-refactored (shipped/audited; refactor = risk, no functional gain).
      - **Corrupt:** setup seed in generateVillainDeck (`sidekickDeck.splice(0,10)` → stamp corruptSidekick + fightEffect gainCorruptedSidekick → push to villain deck; runs after the sidekick expansion-filter). Dynamic Attack = 2 + koPile Scheme-Twist count, keyed on `corruptSidekick` in BOTH attack twins. Defeat → `gainCorruptedSidekick` (fightEffect on the fight COPY) flips it back to a Sidekick (type Hero, secondaryType Sidekick, abilities intact) on the TOP of your deck (skrulled skips VP). Twists 1-7: active player ("each player" solo) returns a Sidekick from discard to the Stack if present, then 2 Sidekicks enter the city (enterCityFromRight). Twist 8: all city Sidekick-Villains escape (direct push + escapedVillainsCount++, no KO/discard penalty). `corrupt4SidekicksEscape` endGame + "N/4 Sidekicks Escaped" tracker twin (both filter `escapedVillainsDeck` for corruptSidekick). **`createVillainCopy` gained `secondaryType` + `originalType`** so the converted gained card is a valid Sidekick (safe for all villains — undefined otherwise).
      - **Gate (rule 7):** expansion-validator **0 issues** (7/7 Golden, twin parity, sanity checks); `/code-review` (high, 3 finder angles + verify) → **0 fixes** (4 findings, all intended-behavior or latent/precedent-matching — see below); **dual-mode `/game-test` ✅**: GOLDEN full (setup 10-inject/stack 44→34; dynamic attack 2+twists re-derived 3→4 live; return-from-discard + 2 enter city; defeat→convert to Sidekick deck-top NOT VP; Twist 8 escape-all; Evil Wins at 4 + defeat popup) + What If? (setup 10-inject; twist 1 return+enter, attack 3; defeat→convert). Console clean (only sw.js 404 + a test-dummy `/undefined` image, not production). Identical across modes (each-player→active + deck-composition divergence verified consistent).
      - **📋 REPORTED (not fixed) code-review observations:** (F1) escaped corrupt-Sidekicks carry type:"Villain" so they count toward the generic end-game/Final-Showdown escaped-villain score stat. **✅ COORDINATOR RULING (2026-06-26): LEAVE AS-IS — they DO count as escaped Villains for the generic stat.** Faithful to the scheme's "Sidekicks in the Villain Deck and city ARE Villains" clause; the scheme's own 4-escape loss is a SEPARATE corruptSidekick-keyed counter and is unaffected. **Phase-4 blind-compare: do NOT flag this — intended/ratified behavior, not a bug.** (F2) gained card carries inert villain residue — mirrors shipped gainScarletWitchAsHero, inert. (F3) return-from-discard can re-grab a recovered Sidekick — intended. (F4) attack-count relies on koPile push-order — correct on the standard twist path Corrupt uses.
    - **3e chunk 2c ✅ DONE (2026-06-26, commits `51b9615` build + `a884453` review-fix): Build an Army of Annihilation.** Rulings cached rules-notes BATCH 8 ④ (twist count 9 ≠ loss 10; ADDITIVE accumulating live board count, NOT the twist-stack snapshot).
      - **Reuse decision (rule 9, deviated from the provisional map — reported & accepted):** reused the **HYDRA-Traitor next-to-scheme system** (`expansionRevelations.js`) — count badge + click-to-fight (pay 3 Attack), depleting supply, "N/threshold" tracker, same 3-Attack cost — NOT the coordinator's two named candidates (`demonGoblinDeck` = shared-global collision with Madelyne; Korvac `isSchemeVillain` = city-slot grid, wrong shape for a 10-count). DEDICATED state (`annihilationSupply`/`annihilationHenchmenNextToMM`/`annihilationTwistStack`) — not the villain deck, not `demonGoblinDeck` — inherently avoids the M.O.D.O.K.s-group / Madelyne collision.
      - **Build:** setup seeds `annihilationSupply=10` in initGame (scheme-gated). `buildAnArmyOfAnnihilationTwist` (expansionSecretWarsVol1.js): (1) recycle — KO all `isAnnihilationHenchman` from the Victory Pile back to supply (player loses that VP, faithful); (2) `annihilationTwistStack++`; (3) place `min(twistStack, supply)` MORE next-to-MM (ADDITIVE). `fightAnnihilationHenchman` (HYDRA-traitor attack-spend, `recruitUsedToAttack`/`showCounterPopup`): pay 3 Attack → defeat → Victory Pile (`makeAnnihilationHenchman`, M.O.D.O.K. stand-in Attack 3/VP 1), no Hero KO. Badge `#annihilation-army-deck` on `#mastermind` (DOM+CSS+render/click in updateGameBoard). `endGame "annihilation10Henchmen"` (≥10 live count) + `updateEvilWinsTracker "Build an Army of Annihilation"` twin "N/10". Zero `gameMode` branch.
      - **Dedicated counter (review Finding 1 fix, `a884453`):** initial build reused the shared `stackedTwistNextToMastermind`, which surfaced a stray `#stacked-mastermind-cards` card-back badge (unconditional render @ script.js:8613). Switched to a dedicated `annihilationTwistStack` (matches `hydraOfficersNextToScheme`/`demonGoblinDeck` precedent) rather than special-casing the shared render. The `stackedTwistNextToMastermind` per-game reset in initGame is KEPT as cheap insurance for a real cross-game leak (FF/GotG/Capture Baby Hope — base bug, **reported**), now decoupled from this scheme.
      - **Gate (rule 7):** expansion-validator **7/7, 0 blocking/0 advisory** (validator independently endorsed the cross-game-leak reset); `/code-review` (high, correctness + cleanup/altitude/conventions angles + verify) → 1 fix applied (Finding 1 stray badge), rest precedent-matched (HYDRA pattern) or inherited-edge (showCounterPopup reject — same as shipped fightHydraTraitor) → report-only. **`/game-test` ✅ PASS (golden, state-injection):** **CRITICAL loss-reachability / anti-snapshot** — count accumulates ADDITIVELY 1→3→6→10 (diverges from twistStack: twist 3 nextToMM 6 ≠ stack 3), reaches 10 by twist 4, **defeat popup fires**; proves the snapshot model (caps at 9, unreachable) is NOT what's built. Fight → VP scoring (+1, pay 3 Attack) → per-twist recycle (defeated VP henchmen → supply, player loses VP) all correct, totals conserved. Badge renders/click-bound, clean placement on mastermind (top-left, no overlap — screenshot `docs/playwright-runs/2026-06-26/swv1-2c-badge-mastermind-zoom.jpeg`). Re-verified post-fix: shared counter stays 0, stray badge hidden every twist. Console clean (sw.js 404 only). **Mode:** zero `gameMode` branch confirmed (grep) → golden-live sufficient per coordinator; What If? identical by construction.
      - **📋 REPORTED (base bug, not fixed beyond the kept reset):** `stackedTwistNextToMastermind` was never reset between games (module-load only) — second game in a session inherited the prior value, affecting Capture Baby Hope / FF / GotG. Patched defensively in initGame; flagged for the coordinator.
    - **3e chunk 2d ✅ DONE (2026-06-26, commits `045fe35` build + `775de4f` review-fix): Master of Tyrants — the LAST Phase 3e scheme. 🎉 PHASE 3e SCHEMES COMPLETE (all 6: Dark Alliance, Crush Them, Pan-Dimensional Plague, Corrupt the Next Gen, Build an Army, Master of Tyrants).** Rulings: rules-notes BATCH 8 ⑤.
      - **Build:** setup seed in generateVillainDeck (`buildMasterOfTyrantsTyrants`): `pickThreeDistinctTyrantMasterminds` (Dark Alliance eligible Core pool excl. main, `shuffle().slice(0,3)`) → stamp each MM's 4 tactics via shared `stampCardsAsInDeckVillains` PER-MM (printed Attack = source MM `attack`; `skrulled:false`; `fightEffect:""`; `isTyrant:true`; `darkPower:0`; `originalAttack:mm.attack`). `masterOfTyrantsTwist`: T1-7 place Dark Power on a player-chosen city Tyrant (`showCardSelectionPopup`; auto if 1, no-op if 0) → `darkPower++`; T8 all city Tyrants escape + captured Bystanders ride with them. **THE BUILD-NEW — persistent villain-identity Dark Power token:** per-villain `darkPower` count (+2 Attack each, cumulative) folded into `attackFromScheme` in BOTH `updateVillain/HQVillainAttackValues` twins (byte-identical); `darkPower`+`isTyrant` whitelisted in `createVillainCopy`. `endGame "tyrants5Escape"` (≥5 `isTyrant` in `escapedVillainsDeck` — any escape, Twist 8 OR overflow) + `updateEvilWinsTracker "Master of Tyrants"` twin "N/5". No DB/HTML/CSS change (reuses the attack overlay). Zero `gameMode` branch.
      - **VP = each Tactic's PRINTED `victoryPoints`** (defeat → normal `!skrulled` Victory-Pile push). **NOT a flat 6** — the eligible Core MMs' tactics are VP **5** in the DB (Dr. Doom/Red Skull/Magneto/Loki all 5), so Tyrants score 5. House interpretation ("printed VP", research-backed); flat-6 would misprice them. **Flagged for coordinator/Paul.**
      - **Gate (rule 7):** expansion-validator **7/7, 0 blocking/0 advisory** (attack-twin parity byte-identical; createVillainCopy complete; no console.log); `/code-review` (high, correctness + cleanup/altitude/conventions + verify) → **1 real fix** (Finding 1: Twist-8 dropped Bystanders captured on a Tyrant → now escape with it, mirrors handleVillainEscape) + 3 cleanups applied (shuffle reuse; dropped inert conditionalAbility/unconditionalAbility:"None"; originalAttack=mm.attack); 1 latent report-only (epic-name exclusion — not reachable, matches Dark Alliance picker). **`/game-test` ✅ PASS (golden, state-injection):** 12 Tyrants from 3 distinct MMs excl. main (Mephisto game; Attack 7/8/10, VP 5, no fightEffect, skrulled:false); Dark Power +2 stacks (→+4, attack overlay "11"=7+4, survives createVillainCopy); chooser targets the chosen Tyrant (2-item popup, only chosen gets it); defeat → VP scores 5; Twist 8 escapes all city Tyrants (5→0) + Bystander rides with (delta 3 = 2 Tyrants + 1 bystander, bystander NOT counted as Tyrant); loss fires at 5 + custom defeat text + tracker "5/5". Console clean (sw.js 404). Mode-agnostic (grep-confirmed zero `gameMode`). Screenshot `docs/playwright-runs/2026-06-26/swv1-2d-tyrant-darkpower-city.jpeg`.
      - **📋 REPORTED (base bug, report-only):** the **Corrupt the Next Generation Twist-8 escape-all (shipped, 2b)** has the IDENTICAL Bystander-drop gap I fixed in Master of Tyrants — a Bystander captured on a corrupt Sidekick-Villain at Twist 8 is orphaned (the manual `city[i]=null` loop bypasses handleVillainEscape's bystander-escape). Low frequency. Flagged for coordinator to decide on a backport to `corruptTheNextGenerationOfHeroesTwist`.
    - **Phase 3e COMPLETE → next: Phase 4 (full `/expansion-audit` + dual-mode gate + `sandbox-review` + Paul playtest + merge). Coordinator note: resolve B18 (randomize ignores requiredHeroes) first — the dual-mode gate needs randomize-start.**
- 3f Henchmen/Bystanders: ✅ **COMPLETE (worker, 2026-06-25). Henchmen (3 groups) — commit `40081cb`; Ghost Racers close-out `64a1591`. Banker bystander — commit pending.**
    - **Banker bystander (×3, VP 1) ✅ DONE.** "When you rescue this Bystander, +2 Recruit, usable only to recruit Heroes in the HQ space under the Bank." Built `getBankHQReserveIndex()` (resize-aware city→HQ-slot resolver) + `bystanderBanker()` in `expansionSecretWarsVol1.js`. DB rows already carried `bystanderUnconditionalAbility:'bystanderBanker'` (no DB/HTML change). **Reuse:** position-keyed reserve-recruit pool (`hq1..hq5ReserveRecruit` via `getHQReserved/setHQReserved`, consumed only on that slot in `showHeroRecruitButton`) — same machinery as FF `moleManUndergroundRiches`; restricted pool, SEPARATE from total/cumulativeRecruitPoints (cumulative-twin deliberately N/A). **The one genuinely-new piece: the resolver** — maps the Bank city space → its HQ reserve slot off the LIVE `citySpaceLabels` (NOT moleMan's hardcoded index, which breaks under resize). Reusable for Madelyne "Bank Robbery" (3d) + "Smash Two Dimensions" (later). **Resolver mapping:** HQ cell under city space C = `C - (citySize - hq.length)`, +1 for the 1-indexed reserve. Default 5-space: Bank idx 3 → reserve 4 (= #hq-4, empirically verified, matches moleMan Streets→hq2). Earthquake 7-space: Bank idx 5 → reserve 4 (offset 2; Bank is the 4th of the 5 canonical spaces). **Gate:** expansion-validator **0 blocking/0 advisory** (7/7; moleMan parity confirmed); `/code-review` (high, 2 finder angles + verify) → 3 findings, ALL comment-only/low (Tsunami comment corrected; left-side-resize assumption documented; the "persists across turns" flag was REFUTED — reserve correctly resets in `endTurn`, same-turn per card + moleMan). **Dual-mode + dual-layout `/game-test` ✅ PASS** (golden default 5-space + whatif Earthquake 7-space): resolver returns 4 in BOTH layouts; +2 grant lands on hq4 only; stacks (×2 → 4); spendable only on the Bank slot (affordable 1+4≥3, non-Bank 1+0<3); spend drains the reserve (combined drop 3) incl. in the resized layout; no-Bank → resolver null → graceful no-op (reserves untouched, rescue completes); empirical geometry characterized (7-vs-5 rows don't pixel-align but hq4 is canonical + nearest). Mode-independent (no `gameMode` branch).
    - **Built (`expansionSecretWarsVol1.js` PHASE 3f section + `cardDatabase.js` henchmen + `index.html` checkbox):**
      - **M.O.D.O.K.s** (id 9, HIGH spec) — `modoksFight`: combined picker over discard-pile + HQ Heroes (`showCardSelectionPopup`); recruit-icon (recruitIcon||recruit>0) captured BEFORE KO → +1 Recruit both totals; HQ KO refills mode-correctly via `refillHQSlot`. KO via shared `koControlledHeroByIdentity`.
      - **Thor Corps** (id 10, LOW spec) — `thorCorpsFight`: reuses the SHARED `gainVillainAsHero` converter (same path as Manhattan villains); DB now carries `gainAsHero:true` (whitelisted in `createVillainCopy`; VP-skip guard at script.js:13581 already named Thor Corps). Hero form Avengers / `["Strength","Range"]` dual-class / recruit 2 / cost 3 / Green. Superpower `thorCorpsBonusRecruit` (`[STRENGTH][RANGED]:+1 Recruit`, condition `"Strength&Range"`, `playedCards`) — reuses base `bonusRecruit()`; **self-exclusion convention** (isConditionMet `slice(0,-1)`) → needs a STR + a RNG symbol on OTHER cards, consistent with the established Finding-line-102 precedent (Thor/Wasp/base Thor).
      - **Ghost Racers** (id 11) — `ghostRacersFight`: reveal-a-[COVERT]-Hero **or** KO one of your Heroes with an Attack icon. Reveal mirrors `revealClassOrWound`; KO pool = your-Heroes-with-attack-icon (Artifacts+Hand+Played, NOT discard) via shared `koControlledHeroByIdentity` + `showCardSelectionPopup`. Four-branch "or" resolution (both-payable→choice / reveal-only / KO-only / neither→no-op). **Fight effect ONLY** — Deadlands Ambush "Rise of the Living Dead" stays deferred.
    - **✅ SCOPE RESOLVED (coordinator/Paul, 2026-06-25) — Ghost Racers Fight effect RATIFIED in-scope; KEPT.** Originally deferred in the analysis/spec/tracker (tied to the Deadlands theme) but present in the inventory as a normal henchman; ratified to keep the built+tested Fight effect. Close-out done (commit pending): (1) **frozen-spec block authored** in `docs/expansion-specs/secret-wars-vol1.md` (Henchmen — Ghost Racers) with the executable assertion + the **KO-pool ruling RATIFIED** via rules-oracle (Core p.15/16: "your Heroes" = hand + played, discard/deck EXCLUDED; cached `docs/rules-notes/secret-wars-vol1.md` BATCH 6); (2) **scope markers updated** (progress lines 9/11, spec scope line, mechanics doc, inventory breadcrumb); (3) **Ambush "Rise of the Living Dead" stays deferred** with the Deadlands set — Ghost Racers has no `ambushEffect` → silent vanilla-henchman city entry, verified (code trace script.js:5426 + live game-test, no throw); (4) **no art** → `Visual Assets/CardBack.webp` placeholder until real art exists.
    - **Gate:** expansion-validator **0 blocking** (2 advisory, both self-flagged in-code: Ghost Racers spec-gap + CardBack placeholder); `/code-review` (high, 3 finder angles + verify) → **0 findings** (confirmed: fn names match DB strings, Ghost Racers selectable via data-set dropdown, no sw.js change needed, `koControlledHeroByIdentity` not duplicative of `FightKOHeroYouHave`). **Dual-mode `/game-test` ✅ PASS** (state-injection, real defeat path via `defeatVillain`; auto-driven popups):
      - **Thor Corps** golden+whatif: real-defeat → NOT in VP (gainAsHero skip), gained Hero in discard with exact form; superpower gate (`Strength&Range` met w/ both symbols, not-met w/ one, not-met by self) + grant (+1 both recruit totals).
      - **M.O.D.O.K.s** golden+whatif: discard-KO + recruit-icon→+1 both totals; HQ-KO + refill (**golden rotation** [splice→rightmost, cards shift left] vs **whatif fill-in-place** — divergence verified) + non-recruit→no recruit.
      - **Ghost Racers** golden+whatif: reveal→no KO; forced KO (no Covert)→attack-icon Hero KO'd; no-op edge (no Covert + no attack-icon)→nothing. Console clean (only benign sw.js 404). Screenshot `docs/playwright-runs/2026-06-25/swv1-3f-henchmen-dualmode.png`.

### Keystone dual-mode `/game-test` — ✅ PASS (2026-06-23), both `golden` + `whatif`
Validated via Apocalyptic Magneto ascension (state-injection; randomize-all start, then direct fn calls):
1. **Escape → secondary** ✅ `apocalypticMagnetoEscape` registers 1 ascended secondary (attack 8, VP 6, 0 tactics, `masterStrike:"magnetoStrike"`); `#mastermind-2` slot renders with "8" overlay + correct art.
2. **Master Strike → both fire** ✅ real `handleMasterStrike` loop fires the main MM's strike AND the secondary's (`magnetoStrike`); `mastermindDefeated` stays false.
3. **One-fight defeat → VP-6 Villain** ✅ spend 8 → ascended Magneto lands in Victory Pile as `type:"Villain", victoryPoints:6`; slot hides.
4. **Win gate** ✅ both directions: win fires ONLY when main MM defeated AND `allSecondaryMastermindsDefeated()`; a live secondary blocks the win even with main defeated.
5. **Golden Assertion 4** ✅ `secondaryMasterminds` referenced nowhere in Final-Showdown/cumulative-points math (grep-confirmed: only master-strike loop, win-gate helpers, registration, slot render, click listener) — secondary is a separate must-kill gate, NOT folded into Showdown points; main 4-defeat→Showdown flow unchanged.
6. **Fight smoke** ✅ `apocalypticMagnetoFight` → X-Men HQ slots selectable, non-X-Men greyed, chosen hero → discard, slot refilled. ⚠️ **but** recruit went 0→+3 (hero.cost), see finding below.

### Findings surfaced this session (for coordinator)
- ⚠️ **Finding A — `recruitXMen` over-credits recruit (pre-existing BASE bug, not introduced here).** `recruitHeroConfirmed` no longer deducts cost (script.js:18843-18389 — caller does via `spendRecruitCost`), but `recruitXMen` still does `totalRecruitPoints += hero.cost` with no offsetting spend → net **+cost** recruit instead of free (net 0). Affects base Magneto "Bitter Captor" tactic AND Apocalyptic Magneto Fight. **RULING (2026-06-23): do NOT fix on this branch** — base-game bug in shared code; base fixes land on a dedicated base-code branch, never on an expansion branch. Catalogued as **B6 in `docs/base-game-bug-audit.md` (master)** with trace + central-fix direction. `recruitXMen` left untouched; SWV1 ships consistent with base. Not a blocker.
- ✅ **Finding B — ascended Magneto wrongly counted as an escaped villain — FIXED + VERIFIED on this branch (2026-06-23).** `handleVillainEscape` pushed Magneto to `escapedVillainsDeck` + incremented `escapedVillainsCount` (script.js:6181) BEFORE the escape effect ascended him, so he fed escape-based loss conditions despite becoming a board-present Mastermind. **RULING:** an ascending Mastermind transforms into a threat, it doesn't get away — must not count as escaped. **Fix:** set `ascendsToMastermind: true` on the Apocalyptic Magneto DB entry (completes the keystone flag already whitelisted in `createVillainCopy` + reset on revert) and guarded the deck-push/count in `handleVillainEscape` with `if (!escapedVillain.ascendsToMastermind)`. Reasoned-interpretation comment added at the guard; stale caller comment at script.js:700 updated. `escapedVillainsCount++` is the sole count chokepoint (grep-confirmed); escapeEffect (ascension) still runs. **Verified** via `/game-test` (golden, state-injection): (1) Magneto escape → count 0→0, no deck push, flag present & read truthy on the city card; (2) regression — normal villain (Doombot Legion) → count 0→1 + pushed. No console errors (only expected sw.js 404). **`/code-review` (3 finder angles):** cross-file consumers fully consistent (guard also PREVENTS a latent Magneto double-count: deck + ascended-MM); ascension path correct, no ordering hazard. Surfaced Finding C below.
- ✅ **Finding C — RESOLVED (3e Dark Alliance, commit `354d314`; `/game-test` PASS).** An ascending villain's captured Bystanders now transfer onto the ascended Mastermind in `ascendToMastermind` (kept, rescued on its defeat); `handleVillainEscape` skips the `escapedVillainsDeck` push AND `handleVillainEscapeActions.handleDiscard` skips the carried-away discard penalty for `ascendsToMastermind` villains. Verified: bystander rides onto the ascended MM, not into escapedVillainsDeck, count unchanged. Rules-notes BATCH 7 Q-C (inference, no printed rule). *Original finding (for history):* 🔶 **attached bystander orphaned into `escapedVillainsDeck` when a villain ascends (NEEDS RULING; NOT introduced by Finding B).** The bystander/plutonium/XCutionerHeroes pushes (script.js:6135-6167) sit BEFORE the ascension guard and run unconditionally. If Apocalyptic Magneto is holding a captured bystander at the moment an escape is forced on him, that bystander is pushed to `escapedVillainsDeck` (counted as a lost/escaped bystander) even though Magneto himself ascends to a Mastermind rather than getting away. Bystander capture (`attachBystanderToVillain`, ~5464) is villain-agnostic, so this is **reachable but conditional** (Magneto must hold a bystander at escape time). **NOT a regression from Finding B** — this push behaved identically before the guard; my change only stopped pushing Magneto *himself*. **Why not auto-fixed:** (a) outside the coordinator-scoped Finding B (villain self-push/count); (b) correct behavior needs a rules call — does an ascended Mastermind keep captured bystanders? — and the naive "skip the push too" would orphan the bystander into limbo (not in deck, not on the new Mastermind, not in city = it vanishes), which is worse. Needs a ruling + a small design decision (ride onto the ascended Mastermind's captured list / release to city / leave as-is). Low frequency. Flagging for coordinator. **RULING (2026-06-23): an ascended Mastermind KEEPS its captured bystanders — they transfer to his captured list, rescued on his defeat (ascension isn't escape).** **DEFERRED to Phase 3e:** do NOT build bespoke attach-wiring now — secondary-MM captured-bystander attachment is required for 3e Dark Alliance (T5-6 main-MM-attach is the same capability); implement Finding C there with that wiring (grab a rules-oracle cite if the SW rulebook addresses ascension-vs-captured-bystanders). 3e lands before the Phase 4 audit/merge, so the shipped expansion is never wrong. **INTERIM:** safe current behavior left as-is (bystander → escapedVillainsDeck, NOT the orphaning naive skip); TODO added at script.js:6135-6167.
- ⚠️ **Finding D — temporary Teleport keyword leaks on granted-but-unplayed hand cards (pre-existing BASE/shared-engine gap; surfaced by Inferno Nightcrawler; NOT introduced here).** The Azazel grant pattern (`card.keywords.push("Teleport"); card.temporaryTeleport = true`) — reused verbatim by `infernoNightcrawlerFight` — relies on end-of-turn cleanup to strip the temp keyword. The `cardsPlayedThisTurn` cleanup (script.js:11616-11622) correctly filters `"Teleport"` from `card.keywords`, BUT the `playerHand` cleanup (script.js:11762-11767) only `delete`s `temporaryTeleport` + sets `keyword3="None"` — it does **not** strip the `keywords` array. Play-time Teleport eligibility reads the array (`card.keywords.includes("Teleport")`, script.js:11288). So a card granted Teleport that the player does **not** play that turn keeps `"Teleport"` permanently → Teleport-eligible on all future turns. **Confirmed by inspection** (code-traced + corroborated by 2 independent reviewers). **Affects the shipped base/Dark City Azazel identically** — pre-existing, not a SWV1 regression; `infernoNightcrawlerFight`'s code is a faithful Azazel reuse. **Recommend (per Finding A precedent — base fixes on a dedicated base-code branch, not the expansion branch):** 1-line base fix — add the same `keywords.filter(k => k !== "Teleport")` to the `playerHand` cleanup at script.js:11762, fixing Nightcrawler AND Azazel. NOT fixed on this branch; SWV1 Nightcrawler ships consistent with shipped Azazel. **Note:** strictly, Nightcrawler's spec assertion ("end turn → temp Teleport cleaned off") fails in the current engine — coordinator may opt to make a 1-line exception here (it's tiny + benefits Azazel) vs. routing to base. Flagging for ruling. Non-blocking. **RULING (2026-06-23): FIX ON THIS BRANCH** (carve-out from Finding A: D breaks SWV1 Nightcrawler's OWN frozen spec assertion, and the only non-duplicative fix is the shared cleanup. Principle: base bug a NEW expansion card's spec depends on → minimal fix on the expansion branch; pure-inheritance base bug → catalogue only). Catalogued as **B7 (master), marked fixed-on-branch.** **FIXED + VERIFIED (commit pending):** added the `keywords.filter(k => k !== "Teleport")` strip to the `playerHand` end-of-turn cleanup in `endTurn()` (script.js:~11766), gated to `temporaryTeleport === true` cards (mirrors the cardsPlayedThisTurn cleanup at ~11620). **`/game-test` PASS (golden, 3 cases):** granted card → "Teleport" stripped + flag cleared (Nightcrawler spec assertion now PASSES); innate-Teleport card (no flag) → "Teleport" RETAINED (untouched); granted+other-keyword → only "Teleport" stripped, other keyword kept. **`/code-review` CLEAN** — both grant sites (Azazel, Nightcrawler) guard against granting to a card that already has Teleport, so a temporaryTeleport card can never carry an innate Teleport keyword; reassignment mirrors the proven existing block. Also fixes shipped Azazel (granted-but-unplayed Teleport now clears at end of turn).

### Findings surfaced this session (Manhattan group, 2026-06-23) — for coordinator
- 🔶 **Finding E — Mr. Fantastic "Ultimate Nullifier" can negate a gain-as-Hero converter → the card vanishes (PRE-EXISTING SHARED behavior; identical for shipped Scarlet Witch; NOT introduced as a new defect).** The gain-as-Hero converter runs as a `fightEffect` inside `collectDefeatOperations`, gated by `promptNegateFightEffectWithMrFantastic` (script.js ~13010). If a player nullifies it, the converter never runs, BUT the `gainAsHero` guard still skips the VP push → the defeated Manhattan villain is neither gained as a Hero nor in the Victory Pile (it disappears). **The shipped Revelations Scarlet Witch (`gainScarletWitchAsHero`, also a `skrulled` fightEffect) has the identical interaction** — the Nullifier negate wraps ANY fightEffect, and `skrulled` skips VP the same way. So this is a shared-engine quirk SWV1 inherits faithfully, not a SWV1 regression, and does NOT break the Manhattan frozen-spec assertions. **Frequency: very low** (requires FF + SWV1 both in play AND the player choosing to nullify a *beneficial* gain-as-Hero — self-harm). **NOT fixed on-branch** (Finding A precedent: pure-inheritance shared behavior → catalogue + base-branch fix). **Recommendation for ruling:** either exclude beneficial gain-as-Hero effects from the Nullifier prompt, or route a nullified gain-as-Hero villain to the Victory Pile (defeated, 0 VP) instead of vanishing — coordinator's call (rules: what should nullifying "gain this as a Hero" do?). Fix should cover BOTH gainAsHero and skrulled at once on a base branch.
- 🔶 **Finding F — Professor X "Mind Control" double-gain on a gain-as-Hero villain (PRE-EXISTING SHARED; identical for shipped Scarlet Witch).** `professorXMindControlGainVillain` is invoked in all three defeat handlers gated only on `hasProfessorXMindControl` (NOT on `skrulled` or `gainAsHero`). After the converter already gained the Manhattan villain as its proper Avengers Hero, Professor X then offers to gain it AGAIN as a grey "cost=attack" Hero → two Heroes from one defeated villain. **Shipped Scarlet Witch (skrulled) has the identical double-gain** (call site doesn't guard skrulled either), so this is pre-existing shared behavior, not a parity gap introduced here. Does not break the Manhattan frozen spec. **NOT fixed on-branch** (same Finding A precedent). **Recommendation:** add `&& !villainCard.skrulled && !villainCard.gainAsHero` to the Professor X call-site condition on a base branch (fixes both at once).
- ℹ️ **Finding G — Ultimate Captain America "color of Hero you have" scope (frozen-spec-backed interim; wants ratification).** Implemented as: count DISTINCT colors among Heroes in play this turn (`cardsPlayedThisTurn`, type Hero, including Cap America itself) → that many +Attack. This is the narrowest reading that satisfies the FROZEN spec assertion ("3 distinct colors in play → +3"); game-test confirms +3 (3 colors) / +1 (1 color), both attack totals. The card text "for each color of Hero you have" is genuinely ambiguous (could mean hand+play, or all owned). **Reversible** (one predicate). Flagging for a rules ruling — if coordinator rules wider (e.g. hand+play), I'll widen the predicate. Not blocking.
- ✅ **Gained-hero image (REVIEWED, no action) — keeping the villain art is CORRECT.** These are same-card dual-side (villain side = hero side, one physical card), mirroring `gainScarletWitchAsHero` which also retains the image. Phase 2 smoke confirmed all Manhattan art loads. The `/undefined` image 404s during game-test were a test-harness artifact of direct city-array injection triggering golden city-shift (duplicated "Phalanx"), not a code defect.
- ✅ **Thor/Wasp superpower self-exclusion (REVIEWED, correct).** `isConditionMet("playedCards", "Range"/"Covert")` excludes the played card itself (`slice(0,-1)`), so [RANGED]+3 / [COVERT]+2 require ANOTHER Range/Covert symbol this turn — standard Legendary superpower semantics, identical to base `ThorBonusAttack`. Intended.

## Phase 4: Validation — ✅ COMPLETE (2026-06-27)
<!-- /expansion-audit + MANDATORY dual-mode gate (multi-mastermind touches rows 4/6/8) + guided test -->

Pre-merge audit + fix pass complete. Audit catalog: `docs/audit-results/secret-wars-vol1-2026-06-26.md`.
Worker gates per fix-group: reuse-first survey → expansion-validator + `/code-review` (high) → dual-mode `/game-test` (where mode-sensitive) → commit. Remaining human gates before merge: `sandbox-review` (separate session) + Paul playtest (both modes). **DO NOT MERGE handled by coordinator.**

### Dispositions

**FIXED (committed on `secret-wars-vol1`):**
- **M1** — Inferno Colossus/Cyclops captured Bystanders route to Madelyne's Demon-Goblin store (`madelyneCapture`/`demonGoblinDeck`) under the default Madelyne+Limbo lead, not the generic `mastermind.bystanders`. Commit `d891691`. Rules: Q7 / rules-notes BATCH 9.
- **M3** — Build-an-Army Annihilation-Henchman fight honors Negative Zone (Recruit-pays-Attack) in both affordability twins + the spend path. Commit `4dac8e5`.
- **M2 — converter cancel-vanish CLASS FULLY CLOSED** (4 converter-reachable Mr. Fantastic / Untouchable negate paths; cancelling a converter's Fight effect now scores it to the Victory Pile at printed VP instead of vanishing). Rules: Q8 / rules-notes BATCH 10. Paths:
  - `collectDefeatOperations` city + HQ defeat — commit `61d32e0`.
  - Dr. Strange "Fight the Future" deck-top fight — commit `76d1cda`.
  - Dark City `freeTelepathicVillainDefeat` (Ghost Rider "Infernal Chains" free-defeat chooser — a nested helper inside `ghostRiderInfernalChains()`, reachable when Infernal Chains targets a villain marked with Professor X's Telepathic Probe; NOT the live Professor-X-probe-click path) + `punisherHailOfBulletsDefeat` deck-top defeats — commit `6fecbcb`.
  - Definitive survey of ALL 9 `promptNegateFightEffectWithMrFantastic` sites confirms no 5th converter-reachable site (Location fights 12980/13166, Mastermind Tactic 16689, and the Burrow branches 13875/14637 are NOT converter-reachable — Burrow is Giganto/Subterranea only; no card carries Burrow + a converter flag).
- **M4 + escaped-state reset** — `updateVillainAttackValues` Portals branch gated on `i >= 0` (off-grid card no longer yields NaN attack/fight-cost); `escapedVillainsDeck`/`escapedVillainsCount` reset in `initGame` (2nd game same session no longer inherits escaped state → Corrupt/Tyrants end-game checks read clean). Commit `df94e0d`.

**CONFIRMED — no fix needed (verified):**
- **M5** — Black Bolt "Silence is Golden" re-grants printed Attack/Recruit. Bounded: full DB enumeration found no targetable no-rules-text card with a variable payout (all 9 multiplier cards excluded by ability/keyword/bonus; the 2 `noRulesText` cards + S.H.I.E.L.D. basics are fixed; no non-numeric attack/recruit). Exposure is latent only (predicate doesn't guard `multiplier`); no in-scope trigger. Leave as built.
- **H1** — Pan-Dimensional Plague `hqWound[]` slot-keying. Ratified (rules-notes BATCH 8 ②: slot-keyed, re-seed each Twist; hero-bonding rejected — it lets Wounds escape the HQ + breaks Wound-Stack accounting). `/game-test` (Golden) of the mid-turn-refill case proved the display token (`hq-N-wound`) and the recruit gate are BOTH slot-keyed and AGREE at every step — no display-vs-charge defect (the audit's concern is refuted). The wound transfers to whoever slides into the slot = the ratified "plague sits on the HQ slot" model. Leave as built.
- **L4** — Namor/Thanos `freeDefeatMastermind`. Dual-mode `/game-test` correct: one Tactic removed per free defeat; graceful no-op at zero. Golden keeps `mastermindDefeated=false` at 0 tactics (Final Showdown still required); What If? marks it defeated (the What If? win) — correct mode divergence. Async-timing nuance (the win flag settles on the next win-check rather than synchronously within the call that pops the last tactic, because `revealMastermindTactic` resolves un-awaited) has NO real-play impact — the per-action `updateGameBoard`/`checkMastermindState` catches tactics===0.
- **M3 Negative-Zone branch — CONFIRMED unreachable (correct/harmless parity).** The Negative-Zone branch in `fightAnnihilationHenchman` / `showAnnihilationHenchmanFightPrompt` cannot fire in normal play. `negativeZoneAttackAndRecruit` is set `true` ONLY by `pullRealityIntoTheNegativeZoneTwist` (expansionFantasticFour.js — the "Pull Reality Into the Negative Zone" scheme); the Annihilation Henchman fight exists only under "Build an Army of Annihilation." One scheme per game → the two never coexist, so the branch is dead in practice. The M3 fix is correct, harmless parity for an impossible state — leave as built.

**CLOSED — working as designed:**
- **H2 / B18** — Golden hero count. GS default = 5; a scheme overrides only if its card states a count, and none of the 6 SWV1 scheme cards do → GS uses 5, What If? uses 3. No defect. (Randomize + validation both read `getEffectiveHeroCount`, so games start cleanly in both modes.)

**DEFERRED → base-code branch (not this expansion branch):**
- `skrulled` / `gainScarletWitchAsHero` (House of M) converter cancel-vanish — a separate rules question, and those terminal cards have NO printed VP (lower impact than the SWV1 converters fixed here). Findings E/F (Mr. Fantastic-negate / Professor X double-gain on gain-as-Hero) belong to this base cluster.
- **Central-refactor candidate** — hoisting the converter-flag clear into `promptNegateFightEffectWithMrFantastic` would dedupe the now-4 per-site mirrors, but it's not a clean drop-in: 5 negate sites call it with NO card arg, and the M2 site passes `(villainCopy, villainCard)` clearing the original — a central fix needs a guarded, param-aware impl. Low drift risk at 4 well-commented mirrors; revisit if a 5th converter-cancel site appears.
- Old Man Logan Loner +2 retained via 4 free-recruit ability paths (recruitHeroConfirmed direct-call bypasses the showHeroRecruitButton guard); floor-at-0 clawback limitation; base-code branch.

**DEFERRED — LOW (SWV1):**
- Untouchable's reactive cancel is NOT wired into the Fight the Future deck-top path (only Mr. Fantastic's negate is) — pre-existing, narrow; Untouchable's text covers "Villain or Mastermind" fights and the FtF deck-top fight predates it.
- The live Professor X Telepathic Probe fight (`initiateTelepathicVillainFight`, cardAbilitiesDarkCity.js ~7028, invoked from script.js ~18431) has NO Mr. Fantastic / Untouchable cancel wiring at all — so a converter's Fight effect can't be cancelled there. No vanish bug (no cancel = no skipped VP push); pre-existing; outside the Q8 converter-vanish scope. Same family as the Untouchable/FtF deck-top gap above.
- LOWs **L1** (Wound counts as no-rules-text), **L2** (Kitty Pryde Tech count lacks `type==="Hero"` guard), **L5** (Inferno multi-mastermind "the Mastermind" picker), **L7** (Corrupt Twist-8 no carry-away discard), **L8** (Corrupt overflow escape `skrulled→Hero` type flip), **L9** (Demon-Goblin lock not parameterized for secondaries) + keyword latents (gainAsHero+Burrow, secondary master-strike silent-skip, `updateEvilWinsTracker` reads setup-DOM scheme) — no in-scope trigger for any.

**RATIFIED (recorded so the blind-compare logs them as intended):** **L3** (Fight the Future at-resolution fight window), **L6** (Inferno Cyclops single carry-away discard).

---

## Open Decisions (block specific cards/schemes; NOT guessed — see mechanics doc Open Questions)
1. **Fragmented Realities — solo shape** (gameplay call, Paul). Degenerate at 1 player.
2. **Build an Army of Annihilation — henchman stand-in** (gameplay call, Paul). "Annihilation Wave Henchmen" not in this box.
3. **gainSidekick() helper design** (technical — propose & surface). Cost-free, cap-free Sidekick-gain helper is genuinely missing.
4. **Dual-class storage** (technical — verify & surface). Mechanics doc recommends 2-element `classes: ["Covert","Tech"]` array, NOT a slash string; must grep script.js for raw `classes[0]` reads before relying on it.
5. **Smash Two Dimensions — parallel-dimension cost** (scope call). High-cost new build; deferral candidate per Core Engine Changes #2.
