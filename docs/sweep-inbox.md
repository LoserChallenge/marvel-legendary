# Sweep Inbox — raw capture & staging

> **Status:** ACTIVE · created 2026-06-19 · the staging surface for `/legendary-sweep`.
> A holding pen for cross-expansion domain learnings pulled from session transcripts.

## What this file is

A **holding pen** for newly-noticed domain learnings — reusable code patterns, design/rules-decision precedents, in-session discoveries (bugs/corrections/gotchas), and recurring pre-flight gaps. Capture is cheap and append-only.

## What this file is NOT

**It is staging, not a recall surface.** Nothing here auto-loads into any session. An item protects against nothing until it is promoted out of this file to its canonical home. The inbox exists to be **drained toward empty**, not to accumulate.

## What this sweep is FOR (and not)

This sweep is deliberately narrow. Legendary already documents well — `/plans`, `/specs`, `expansion-mechanics/`, `expansion-progress/`, `known-issues.md`. The sweep does **not** duplicate those. It captures the two things those docs miss:

1. **Cross-expansion reuse memory** — the "we already solved this in expansion X, reuse the pattern/precedent" thread that no single per-expansion doc owns.
2. **In-session discoveries that never got written down** — bugs found while fixing, corrections made while testing, conventions established on the fly. These live in the transcript but evaporate from memory when a session is cleared.

## The three fates (every item ends as exactly one)

1. **Promoted** → graduates to its canonical home (see routing table) → **deleted from inbox.**
2. **Pruned** → not durable → **deleted from inbox.**
3. **Pending** → stays *only* until it earns a verdict.

A healthy inbox holds **only category 3** (+ `#pin`).

## Capture format

One line per item: a date, one tag, a one-line statement, optional detail.

```
- YYYY-MM-DD #tag — <one-line statement>. <optional detail / why it matters>.
```

**Tags:** `#pattern` (reusable code pattern / helper / recurring-bug rule) · `#decision` (rules-interpretation or design-decision precedent) · `#gotcha` (empirical in-session discovery not yet in any doc) · `#gap` (recurring deferred item / pre-flight gap spanning expansions). Add `#pin` as a second tag to mark an item **durable even if rare** (exempt from dwell-time pruning).

## Promotion targets (route each keeper)

Promotion is **consolidation, not addition** — refine the existing rule at the destination; never add a parallel second rule.

| Kind of learning | Canonical home |
|---|---|
| Deep engine code trap / reusable pattern (`#pattern`, `#gotcha`) | `docs/engine-gotchas.md` (on-demand reference; CLAUDE.md keeps only highest-frequency rules inline + a pointer) |
| Cross-expansion design/rules-decision precedent (`#decision`) | `docs/expansion-decisions.md` |
| Card-content rules ruling | `docs/rules-notes/` (cited to source) |
| Recurring pre-flight gap across expansions (`#gap`) | `docs/known-issues.md` (or a pre-flight checklist consumed by `/analyze-expansion` / `/new-expansion`) |
| Data / inventory convention | `docs/card-inventory/card-reading-rules.md` |
| CC/workflow meta-learning | **NOT here** → cc-helper's global `learning-sweep` |

---

## Capture log

<!-- Append new items below this line. Newest at top. Drain toward empty. -->

<!-- CAPTURED 2026-06-29 (Pass A): 27 new transcripts swept (Secret Wars Vol. 1 build, 06-22 → 06-29). De-duped from ~110 raw subagent lines; CC/workflow-meta excluded. Pass B (promote/prune) pending Paul's approval. -->

- 2026-06-29 #gotcha #pin — Two near-duplicate "Professor X Telepathic Probe" defeat functions exist: `freeTelepathicVillainDefeat` (darkcity, nested INSIDE `ghostRiderInfernalChains()`, HAS negate wiring) vs `initiateTelepathicVillainFight` (darkcity ~7028, the live probe-click path, NO negate wiring). The path logged as "Professor X" was actually Ghost Rider "Infernal Chains" free-defeat. When labeling converter-reachable negate paths, verify the enclosing function, not the log string.
- 2026-06-29 #gotcha — Live Professor X Telepathic Probe path (`initiateTelepathicVillainFight`) has no `promptNegateFightEffectWithMrFantastic` call at all — no converter-cancel and no vanish bug on it; gap is pre-existing, deferred-LOW (same family as Untouchable / Fight-the-Future deck-top).
- 2026-06-29 #gotcha — `negativeZoneAttackAndRecruit` is set true ONLY by `pullRealityIntoTheNegativeZoneTwist` (FF scheme); the Annihilation Henchman fight exists only under "Build an Army of Annihilation." One scheme per game → the M3 Negative-Zone branch is unreachable in normal play. Confirm two schemes can coexist before assuming a cross-scheme-flag branch is live.
- 2026-06-29 #pattern — When an expansion ability must count a card type the shared `otherRealCardsPlayedThisTurn()` helper strips by design (e.g. Sidekick residue stamped `sidekickToDestroy`), add an opt-in boolean param (default false) to the shared helper rather than copying filter logic. Magik "Dimensional Portal" needed this (`includeSidekickResidue`); 8 existing callers stay unchanged.
- 2026-06-29 #gotcha — Old Man Logan "Loner" (+2 Attack if you don't recruit a Hero) clawback floors at `Math.max(0, total-applied)`: spend the +2 on a fight before recruiting and the floor swallows the debt — player keeps both. Fix = early-return guard in `showHeroRecruitButton` before any spend. Residual: 4 free-recruit paths bypass the guard (see free-recruit-bypass item).
- 2026-06-29 #gap — Hero-card auditor checks text-vs-code alignment but cannot catch runtime condition bugs (a predicate evaluating wrong during play); those require a playtest or game-test that actually triggers the ability.
- 2026-06-28 #gotcha — `returnToSidekickDeck()` (cardAbilitiesSidekicks.js) stamps every played Sidekick copy with `sidekickToDestroy:true` at play time; the shared `otherRealCardsPlayedThisTurn()` helper excludes those — so any ability counting "Sidekicks played this turn" via the helper always returns 0. Fix via opt-in `includeSidekickResidue` (default false preserves callers).
- 2026-06-28 #gotcha — Provisional-grant + floor-at-0 clawback is a recurring exploit class: if the granted bonus is spent before the conditional event, `Math.max(0, total-applied)` swallows the debt and the player keeps both. Gate at the action surface (e.g. `showHeroRecruitButton`) before any spend, conditioned on `appliedBonus>0 && totalPoints<appliedBonus`; and floor BOTH `totalAttackPoints` and `cumulativeAttackPoints` (a negative cumulative corrupts Final Showdown scoring).
- 2026-06-28 #gap — Four free-recruit ability paths (`doomHeroRecruit`, `recruitXMen`, `freeHeroGain` in cardAbilities.js; Spider-Woman in expansionPaintTheTownRed.js) call `recruitHeroConfirmed` directly, bypassing `showHeroRecruitButton` and any guard placed there. Per-recruit / conditional-bonus enforcement at that chokepoint can't cover them without per-ability pre-gates or a spend-order refactor (tracked WATCH-1).
- 2026-06-28 #gotcha — Henchmen setup list is static HTML (hardcoded checkboxes in `index.html`), NOT data-driven from `cardDatabase.js`; both manual selection and `randomizeHenchmen()` read only DOM nodes. Hiding a henchman = comment out its `<label>` in index.html; deleting/excluding the DB entry has no effect on setup population.
- 2026-06-27 #pattern — Any twist effect that manually loops city slots and nulls them (escape-all pattern) must also escape attached Bystanders via the same logic as `handleVillainEscape`; omitting it silently orphans captured Bystanders. Hit twice: Master of Tyrants and Corrupt the Next Generation.
- 2026-06-27 #pattern — Every "Mastermind captures a Bystander" effect must check `unfightableWhileDemonGoblins===true` (Madelyne's DB marker) and route through `madelyneCapture()`/`BystanderstToDemonGoblins()` instead of generic `mastermind.bystanders`. Check villain ambush effects, villain escape effects, and scheme-twist capture helpers.
- 2026-06-27 #decision — When `Untouchable` cancels a converter card's Fight effect, the defeat still stands (Core p.13: defeat→Victory Pile is a separate step from the Fight effect). Fix by clearing route-away flags (`gainAsHero`/`corruptSidekick`) at the shared `collectDefeatOperations` hook so the existing VP-push gate fires — do not build a parallel scoring path.
- 2026-06-27 #gotcha #pin — The `skrulled` flag is shared across mechanics: SWV1 converter cards, Skrull Shapeshifters `unskrull`, and House of M `gainScarletWitchAsHero`. Any fix keyed on `skrulled` must narrow to expansion-specific markers or it silently alters other expansions' behavior.
- 2026-06-27 #gotcha — `getEffectiveHeroCount` treats `requiredHeroes===3` as a sentinel meaning "use mode default" (5 in Golden); a deliberate literal `requiredHeroes:3` is indistinguishable from the legacy unset value. SWV1's 6 schemes correctly start with 5 heroes because no SWV1 scheme card specifies a count (Golden-Solo base → scheme-card override hierarchy).
- 2026-06-27 #gotcha — An off-grid villain (`cityIndex/i = -1`) under "Portals to the Dark Dimension" reads `cityPermBuff[-1]` = undefined → sets `attackFromScheme`/villain attack to NaN. Affects both `updateVillainAttackValues(card,-1)` and `recalculateVillainAttack`; current readers mask with `||0` but future off-grid callers need an `i >= 0` guard at the `cityPermBuff` read site. Guard locally (printed-Attack fallback) in any function that fights an off-grid villain.
- 2026-06-27 #decision — Pan-Dimensional Plague `hqWound[]` is slot-keyed (ratified): both the display token and the recruit gate key off the slot index so they always agree; on a mid-turn HQ slide the Wound visually transfers to whoever occupies the slot. Hero-bonding was rejected because it lets Wounds escape the HQ in Golden, breaking Wound-Stack loss accounting.
- 2026-06-27 #gotcha — `freeDefeatMastermind` (What If? mode): `mastermindDefeated` does NOT set synchronously when the last Tactic is removed — it settles later because `handleMastermindPostDefeat` calls `revealMastermindTactic` un-awaited. Normal play handles the lag via turn-flow `updateGameBoard`/`checkMastermindState`; automated test sequences must account for the one-step lag.
- 2026-06-26 #gotcha #pin — "Build an Army of Annihilation" loss mechanic is ADDITIVE accumulating (1+2+3+4=10 next-to-MM by Twist 4), NOT snapshot (count==twistStackSize). Snapshot caps at 9, making the 10-loss unreachable. Game-test confirmed divergence at Twist 3 (nextToMM=6, twistStack=3).
- 2026-06-26 #gotcha #pin — Cross-game state leak: several globals are never reset in `initGame` and retain state on a 2nd game in the same page session (no reload) — `demonGoblinDeck` (hard fight-lock for Madelyne / demon-goblin expansions), `secondaryMasterminds`, `escapedVillainsDeck`/`escapedVillainsCount`, `stackedTwistNextToMastermind`, per-expansion next-turn flags (e.g. Sentinel). Add every new global flag to `initGame`'s per-game reset cluster; for `demonGoblinDeck` reset in `initGame`, NOT `initializeDemonGoblinDeck` (gated by an init flag that skips on game 2).
- 2026-06-26 #gotcha #pin — `defeatSecondaryMastermind` (script.js:17102) pops each Tactic straight to the Victory Pile and NEVER calls `resolveTacticEffects()`; the main-MM path (script.js:16369) does. Any expansion placing a full secondary Mastermind with Tactic fight effects (e.g. Dark Alliance) must wire `resolveTacticEffects` into the secondary defeat path first.
- 2026-06-26 #pattern — Off-grid fightable pools next to the Mastermind/scheme: the HYDRA Traitor "officers next to scheme" system (Revelations) is the canonical reuse template (count badge, click-to-fight, depleting supply, N/threshold tracker, 3-Attack fight cost). Use dedicated per-scheme state to avoid colliding with `demonGoblinDeck`/`stackedTwistNextToMastermind` (the latter's badge render at script.js ~8613 is unconditional).
- 2026-06-26 #pattern — `stampCardsAsInDeckVillains(sourceCards, stamp)` is the shared inject-as-villain helper for `generateVillainDeck` seeds; generalizes Skrull (@4475) + House of M (@4785). Default stamp: `type:"Villain", attack:0, skrulled:true`, captures `originalAttack`/`originalType`; per-scheme overrides via the stamp arg.
- 2026-06-26 #gotcha — `handleVillainEscape` applies a hero-KO penalty on EVERY escape; for scheme twists that escape villains WITHOUT that penalty, push directly to `escapedVillainsDeck` + increment the counter — do NOT route through `handleVillainEscape`.
- 2026-06-26 #gotcha — `handleVillainEscape` pushes captured Bystanders to `escapedVillainsDeck` BEFORE the escapeEffect runs (forEach at script.js:6143-6167); for an ascending villain, guard the push — transfer `villain.bystanders` onto `sm.bystanders` (rescued via `collectMastermindRescueOperations` on defeat) instead of orphaning them in `escapedVillainsDeck`. (Deferred to the Dark Alliance / secondary-MM bystander phase.)
- 2026-06-26 #decision — Convert-on-defeat uses the `gainScarletWitchAsHero` template when a scheme converts the just-DEFEATED card to hand/deck; `gainSidekick("deckTop")` pulls a FRESH sidekick from the Stack and is wrong when the scheme requires gaining the card just defeated.
- 2026-06-26 #decision — Dark Alliance eligible secondary-Mastermind pool = Core-only masterminds via an opt-in `darkAllianceEligible:true` DB flag; `alwaysLeads` is universal to all masterminds and functionally inert for a mid-game tactics-only add (not a gating criterion).
- 2026-06-26 #decision — Bystander-capture source for scheme twists = the Bystander Stack (`bystanderDeck.pop()` with empty guard), NOT the villain-deck bystander flow (Core p.15: the Stack is the supply).
- 2026-06-26 #pattern — Two distinct scheme-twist dispatch paths: `handleSchemeTwist` (no arg; `schemeTwistCount` already incremented at 6086, effect at 6108) vs `handlePlutoniumSchemeTwist` (passes villainCard; plutonium-gated). A twist/"place-N-into-pool" helper generalizing across both needs a safe default for the count/villainCard param to avoid object-as-number bugs on the no-arg path.
- 2026-06-26 #pattern — Mastermind fightability-lock wires to 4 surfaces: `handleMastermindClick` + the two `canAttackMastermind` twins + `canAttackMastermindNZ` (Negative-Zone variant), all via `getSelectedMastermind()`. Clone the `isMastermindRecruitLocked()` pattern and add a DB flag to scope the lock. The 3-gate city-villain pattern (`usesRecruitToFight` twins) is WRONG for masterminds.
- 2026-06-26 #gotcha — If a frozen spec cites a wrong reuse path and the implementation correctly diverges, add an explicit spec-correction note at that spec line, or Phase-4 blind-compare false-flags the correct implementation against the wrong spec text.
- 2026-06-26 #decision — "Had X before this action" conditionals: snapshot the value BEFORE the action that changes it (e.g. `const priorGoblins = demonGoblinDeck.length` before capture; trigger the Master Strike wound only `if (priorGoblins > 0)`).
- 2026-06-26 #decision — Ghost Racers "KO one of your Heroes" zone (Core p.15/16, SETTLED): controlled Heroes = hand + played-this-turn only; deck, discard, Victory Pile excluded. The attack-icon filter narrows the pool but does not widen zones; if no eligible attack-icon Hero exists in scope, the branch cannot fire.
- 2026-06-26 #gotcha — A henchman group's Fight and Ambush effects can have different mechanic dependencies: build the independent Fight, defer the Ambush with the rest of the dependent set, and document the split with a commit breadcrumb in the inventory. An unbuilt Ambush handler is a silent no-op (the henchman enters as a vanilla henchman) — confirm by tracing the ambush dispatch path before treating it as a blocker.
- 2026-06-25 #gotcha #pin — `isConditionMet` excludes the playing card via `slice(0,-1)`, so NO card can self-satisfy its own class gate; dual-class cards (e.g. Thor Corps STR/RNG) cannot self-satisfy `Strength&Range`. Settled convention across all shipped cards — never add a dual-class self-satisfy exception.
- 2026-06-25 #gotcha #pin — Master Strike effects that use a popup MUST wrap with `setTimeout(0)` at the top: `showPopup`'s `onConfirm` calls the ability synchronously then immediately `closePopup()`, closing the inner ability popup before it resolves. The TACTIC path is safe (it calls `closeInfoChoicePopup()` before `resolveTacticEffects`).
- 2026-06-25 #pattern — Henchman-that-becomes-Hero pattern: DB `gainAsHero:true` + `gainVillainAsHero()` converter + whitelist in `createVillainCopy` + VP-skip guard at script.js:13581. Henchman defeat already routes through `collectDefeatOperations`, so the flag fires automatically with no extra wiring.
- 2026-06-25 #gotcha — Card inventory and frozen spec CAN diverge (Ghost Racers: present in inventory, explicitly deferred in spec). Check both and flag the contradiction rather than implementing from inventory alone.
- 2026-06-25 #gotcha — HQ city-space label ordering in static index.html (Bridge/Rooftops/Streets/Bank/Sewers) disagrees with `CITY_SPACE_DEFAULTS` (Bridge/Streets/Rooftops/Bank/Sewers), but static labels are overwritten to "HQ-N" at render — the static ordering is a dead red herring. Under the live engine, city column i always maps to hq[i] (0-indexed) = `hq(i+1)ReserveRecruit`.
- 2026-06-25 #gotcha — Under city resize (e.g. Earthquake 7-space city) the HQ stays 5 slots and extra spaces are added on the LEFT; the canonical Bank→HQ mapping must use `hqSlot0 = bankCityIndex - (citySize - hq.length)`. Naive `bankCityIndex + 1` breaks whenever Bank shifts right; the `moleMan` FF function hardcodes the default-layout position and is wrong under resize (pre-existing FF latent bug).
- 2026-06-25 #gotcha — `showHeroDeckEmptyPopup` is CALLED (script.js:5254 What If? branch of `refillHQSlot`; expansionPaintTheTownRed.js:1734) but NEVER DEFINED — any What If? card triggering HQ refill on an empty hero deck throws a ReferenceError. Workaround: check deck length before calling `refillHQSlot`; log the base bug, don't patch base code on a feature branch.
- 2026-06-25 #pattern — Fight-effect cancel scope for Untouchable / MrFantastic-style cards: hook exactly two places — `collectDefeatOperations` (all villain/henchman defeats) and `resolveTacticEffects` (MM tactics). Locations and Burrow keyword sites are out of scope. The MM-tactic hook must NOT be nested inside a `typeof promptNegateFightEffectWithMrFantastic === "function"` guard (the villain hook is FF-independent; the tactic hook must match). The Mysterio tactic exclusion is inherited by any reuse.
- 2026-06-25 #decision — To fold a conditional ability into an unconditional one, set DB `conditionalAbility:"None"` so the engine cannot fire both on the same play; move the threshold check inside the unconditional function body.
- 2026-06-25 #gotcha — "Scatter the Mutants"-style operations: call `refillHQSlot(idx)` BEFORE `heroDeck.unshift(card)`. Unshift first and `goldenRefillHQ` internally `heroDeck.pop()`s the just-scattered card back into the HQ slot — it silently vanishes from the intended bottom-of-deck position.
- 2026-06-25 #gotcha — `recruitHeroConfirmed` handles BOTH hero recruits AND bystander rescues (e.g. under Save Humanity); any ability hooked there must gate on `hero.type === "Hero"` or it fires on bystander rescue.
- 2026-06-25 #pattern — `defeatBonuses()` (cardAbilities.js) is the universal async villain/henchman-defeat hub — the correct hook for any expansion effect triggering on ANY defeat. Do not wire per-defeat effects elsewhere.
- 2026-06-25 #gotcha — `freeDefeatMastermind()` must use the full engine path `createMastermindCopy → collectMastermindRescueOperations → handleMastermindPostDefeat`, not a hand-rolled partial; a partial misses captured-bystander rescue, shards, and `rescueExtraBystanders`.
- 2026-06-25 #gotcha — `secondaryAvailablePoints()` ignores `mastermindReserveAttack` and Bribe/forcefield modifiers (the main fight path spends `mastermindReserveAttack` first; the secondary spend path does not). Latent for Apocalyptic Magneto (plain Attack 8); revisit when a secondary with reserve attack or Bribe is built.
- 2026-06-25 #gotcha — `updateSecondaryMastermindSlot` renders only ONE secondary mastermind (first undefeated via `.find()`); the `secondaryMasterminds[]` data model is a general array but the board UI is single-slot. Simultaneous multiple active secondaries need board-rendering rework.
- 2026-06-25 #gotcha — A full (non-ascended) secondary Mastermind defeat pushes a minimal stub `{name, type:"Mastermind", victoryPoints, image}` to the Victory Pile, not a full card object; a later card/scorer reading fields beyond those four off a VP Mastermind fails silently.
- 2026-06-25 #gap — Frozen-spec line numbers for engine functions drift after edits; reuse-map line references are unreliable past the commit where written — always Grep for current locations before trusting a cited line number.
- 2026-06-25 #pattern — "For each color of Hero you have" effects reuse `CaptainAmericaCountUniqueColorsAndAddAttack` (cardAbilities.js ~1416-1447): distinct `card.color` across `playerHand + playerArtifacts + cardsPlayedThisTurn` (+5 cap implicit). Engine convention: "your Heroes" = hand + play area + artifacts; deck/discard/VP excluded. Ultimate Captain America is a reskin of Core "Captain America – Perfect Teamwork" and must reuse this (a bespoke play-only counter under-counts unplayed colored Heroes in hand).
- 2026-06-25 #decision — Fight-the-Future / deck-top-villain fights: at-resolution popup-fight (Option A, cloned from `punisherHailOfBulletsDefeat`) is the correct solo approximation over a persistent deck-top fightable target (Option B). Documented narrowing: player must have attack ready when the card resolves; cannot reveal-then-build-attack-then-fight-later. Mark as a Phase 4 blind-compare receipt.
- 2026-06-25 #gotcha — `punisherHailOfBulletsDefeat()` (cardAbilitiesDarkCity.js) is the shipping template for reveal-villain-deck-top + popup + `defeatNonPlacedVillain` — the correct reuse target for any card fighting a villain off the top of the villain deck.
- 2026-06-25 #gotcha — Deck-top/off-grid villain fights must mirror the engine's Bribe branch: Bribe villains use recruit (not attack) as fight currency, and `usesRecruitToFight` is position-derived only (`= isBankOrStreets(i)` at script.js:10635; never a static DB flag) so index -1 always evaluates false. Implement Bribe affordability/payment as an explicit inline branch, not by passing an off-grid index.
- 2026-06-25 #decision — Black Bolt "no rules text" predicate: hybrid — an explicit `noRulesText:true` DB flag on intentionally-blank named cards (Speak No Words, Inspiration Through Power) honored first, PLUS a runtime `cardHasNoRulesText()` fallback (no ability/conditional/keyword/bonus) for game-wide no-text cards. Ability-only checks false-positive on keyword-only cards; pure DB-flag misses vanilla S.H.I.E.L.D. cards.
- 2026-06-25 #pattern — `koControlledHeroByIdentity` is the shared helper for "KO a Hero by object identity" across HQ/played/artifacts/hand/discard with an `attackIcon` filter; distinct from `FightKOHeroYouHave` (identity-based, fixed pool, no HQ/discard/attackIcon support). Justify any new KO helper against both.
- 2026-06-24 #gotcha — `KO1To4FromDiscard` is discard-only (1–4 cards, returns nothing); CLAUDE.md's "hand+discard" pointer is imprecise. A card needing a combined hand+discard KO pool that RETURNS the KO'd card is a genuine build-new helper (`koOneFromHandOrDiscard`).
- 2026-06-24 #gotcha — There is no Mastermind free-defeat path and no 4-defeat integer counter: golden "4 defeats" = the 4 tactics; a defeat = tactic-pop + `checkWinCondition()`. Free-defeat cards (Imperius Rex / Utter Annihilation) implement it via tactic-pop, not a counter or a dedicated defeat function.
- 2026-06-24 #decision — Solo handling of "each other player" effects (Q1, provisional/per-card): NO-OP for effects that pull from OTHER players' Victory Piles or have no solo equivalent (Maximus Inhuman Mastery, Thanos Galactic Domination, "everyone's a Demon", "#Humblebrag"); SELF-APPLY for reveal-or-suffer effects the active player can resolve alone (Corrupted Clone, Nimrod Adapt&Destroy, "reveal an X-Men Hero or discard to 4"). Correction: the Phase 2.5 example list miscategorized the VP-pull pair. Mark rulings as interpretations in code comments.
- 2026-06-24 #decision — Teleport keyword grammar (SW rulebook): "Teleport." = OPTIONAL either/or — choose teleport → card set aside, abilities SKIPPED; choose play → abilities run, no teleport. Riders fire on the play branch only UNLESS the card explicitly reads "when you play OR Teleport" (Nightcrawler "Along for the Ride"). Do NOT generalize the Nightcrawler both-fire override.
- 2026-06-24 #gotcha — A Teleport set-aside card joins next turn's hand as a bonus card via `cardsToBeDrawnNextTurn`/`markedToDrawNextTurn` — NOT placed on top of the deck. Inventory glosses saying "deck top" are wrong; the engine's `teleport()`/`playOrTeleport()` implements the correct hand-next-turn behavior (rulebook governs over the inventory paraphrase).
- 2026-06-24 #gotcha #pin — Recurring card-code bug patterns caught in code review of the 3b backbone: (1) `playerDeck.push(sk)` outside the `idx >= 0` guard duplicates a Sidekick (King of Wakanda); (2) using a stale positional `_idx` instead of an identity-based index `city.indexOf(...)` (Mental Domination); (3) scanning raw `cardsPlayedThisTurn` instead of `otherRealCardsPlayedThisTurn` which strips simulation/copy residue (No More Heroes). Guard all three in future expansion card code.
- 2026-06-24 #gotcha — The `skrulled` flag cannot be reused for gain-as-hero conversion in Manhattan: under "Skrull Shapeshifters" it misfires and every Manhattan villain wrongly gains `attackFromScheme = cost+2`. A dedicated `gainAsHero` flag is required (a valid reuse-first exception).
- 2026-06-24 #gotcha — Rachel Summers' global attack delta must be fed through `attackFromScheme` in BOTH attack-setter twins (`updateVillainAttackValues` / `updateHQVillainAttackValues`), NOT at the `recalculate*` cost-sum sites. The city/HQ attack overlay renders from the `attackFromScheme` field-sum; inserting the delta only at recalc makes the fight cost reflect it but the on-screen number not. Overlay and cost must read from the same source.
- 2026-06-24 #decision — A brief or prior worker claiming "pre-existing flag X already exists" must be verified in-code before trusting: the Sentinel Territories brief claimed a "Colossus Q2 next-turn-skip flag" existed — it did not (that flag was this group's work). Reuse-scout-first is mandatory precisely because handoff descriptions propagate errors.
- 2026-06-23 #pattern — When a secondary mastermind is deeply mismatched with the single-mastermind fight pipeline (30+ `getSelectedMastermind` call sites), the correct architecture is an isolated secondary slot with its own minimal fight path, NOT threading an index through all existing code (a "simple > complete" call).
- 2026-06-23 #gotcha — `ascendToMastermind`/`ascendsToMastermind` was scaffolded (whitelisted in `createVillainCopy`, reset-on-revert at script.js:16819) but never wired — the DB never set it true and `handleVillainEscape` never read it. Always check for vestigial scaffolding before building new machinery; adding the DB flag + reading it at the escape chokepoint completes the design.
- 2026-06-23 #decision — An ascending villain should NOT count as an escaped villain: guard the `escapedVillainsCount++` / `escapedVillainsDeck` push in `handleVillainEscape` (~script.js:6185, grep-confirmed single increment site) with `if (!escapedVillain.ascendsToMastermind)`. An ascended Mastermind keeps any Bystanders the villain had captured (transferred to its captured list, rescued on its defeat).
- 2026-06-23 #gotcha — Expansion-validator static analysis flagged the `escapedVillainsDeck` splice inside Inferno Cyclops's escape as "dead code" — FALSE; `handleVillainEscape` pushes bystanders to `escapedVillainsDeck` (script.js:6143-6150 forEach) and a live game-test confirmed the splice fires (deck 3→1). Verify static "dead code" claims with a game-test before removing.
- 2026-06-23 #decision #pin — Base-bug categorization: a base bug a NEW expansion card's frozen spec depends on → apply a minimal fix on the expansion branch (Finding D carve-out). A pure-inheritance base bug (card reuses existing behavior that's also wrong for existing cards) → catalogue only, fix on a dedicated base-code branch (Finding A rule). The distinction is whether the expansion's own spec assertion fails. Pre-existing shared-engine limits an expansion merely exposes are catalogued in the progress doc and deferred to a base-code branch.
- 2026-06-23 #gotcha — The Azazel grant-Teleport pattern (`keywords.push("Teleport")` + `temporaryTeleport=true`) has a cleanup gap: `endTurn()`'s `playerHand` cleanup deletes `temporaryTeleport` (~script.js:11767) but does NOT strip "Teleport" from `keywords` (the `cardsPlayedThisTurn` cleanup at 11616-11622 does). Since eligibility reads `keywords.includes("Teleport")` (11288), a granted-but-unplayed card keeps Teleport permanently. Fix: add a `keywords.filter(k=>k!=="Teleport")` at the playerHand cleanup, gated to `temporaryTeleport` cards.
- 2026-06-23 #decision — `recruitXMen()` has a pre-existing over-credit bug: it skips `spendRecruitCost()` but still adds `hero.cost` back via `totalRecruitPoints +=`, netting +cost instead of free (net 0); also affects base Magneto "Bitter Captor". Central fix = drop the `+=` and its negative-zone twin in `recruitXMen`; catalogue and fix on a dedicated base-code branch, don't patch from an expansion branch.
- 2026-06-22 #decision — "Annihilation Wave Henchmen" (Build an Army of Annihilation) is a printing error — the named group was cut pre-release and exists in no Legendary product. Community consensus (no official errata) is to substitute any henchman group; M.O.D.O.K.s is the default stand-in (rules-as-intended, not a house rule).
- 2026-06-22 #decision — Multiple/ascending Masterminds: ascended masterminds have no Tactics, are defeated in one fight at printed villain Attack, then become Villain cards in the Victory Pile again (source: What If? Rulebook p.19 — fuller than the Secret Wars insert, which only says "defeat all to win"). Golden Solo adaptation: main mastermind keeps its 4-defeats→Final Showdown; secondary masterminds are additional must-kill gates whose Master Strikes fire alongside the main one but are not folded into Showdown math.
- 2026-06-22 #decision #pin — Scheme-scope gate (decide before setup-screen registration so deferred content stays absent): Fragmented Realities has no natural 1-player solo form (per-player-dimension) → skip entirely, not defer; Smash Two Dimensions Together needs a genuine parallel-dimension build (2nd city row + per-entry placement) → defer until prior art exists.
- 2026-06-22 #decision — Staging images (`expansions/`) are gitignored and live only in the main folder, not in worktrees; when Phase 1 image import runs in a worktree the coordinator (which owns the main folder) must copy the staging folder into the worktree path before the worker starts (safe because gitignored; worker must not cross-folder-copy).
- 2026-06-22 #gotcha — `classes[0]` single-index reads are absent across the entire production JS codebase — every class consumer uses `.includes()`/`.some()`/`for...of`/spread, and the icon renderer does `card.classes.slice(0,3).forEach(...)`. Dual-class via a 2-element `classes` array is safe with no engine change.
- 2026-06-22 #decision — `gainSidekick()` helper design: `async gainSidekick(destination = "discard")` where destination ∈ {discard, hand, deckTop}; does NOT touch `sidekickRecruited` or recruit points (gain ≠ recruit) and must include an empty-stack guard. Distinct from `recruitSidekick()` (charges Recruit, burns the one-per-turn cap).
- 2026-06-22 #decision — Epic mastermind sides: Madelyne Pryor and Nimrod (Secret Wars Vol. 1) have no Epic sides on the physical cards — ship standard, no future TODO; the Epic toggle auto-hides when no `epic{}` block is present (`script.js:973` guards `if (mastermind.epic)`).

<!-- DRAINED 2026-06-22 (Pass B): all 31 captured items resolved. PROMOTED → expansion-decisions.md (derive-mechanic-novelty-from-code-search, w/ Shards example) and card-reading-rules.md (title-from-reference-not-filename; hero rarity→copy mapping + non-standard distributions; inventory pass-execution scaling). PRUNED as already-canonical in CLAUDE.md (reference-first protocol [#pin], class/team-from-alt-text, mastermind-tactic-VP-inheritance, checkEvilWins/updateEvilWinsTracker twin, console.log baseline=0, cumulative attack+recruit pattern, Phase-2.5 frozen specs). PRUNED as resolved/recorded in their inventory files (WWH Skaar=Strength, Infini-Tendrils 4+, Korg two-icons, spellings, Destiny Force notation, Sentry hero→MM transform, double-sided-no-Epic MMs, Feast/Trap! gap; New Mutants Karma=X-Men; SHIELD Taskmaster printed 3+/4 + Ranged→Range already done; Messiah Silver Samurai, Evil Clones=Hero Deck, Boom-Boom 0+, Epic Bastion="enemies"). PRUNED as out-of-scope → cc-helper learning-sweep (Phase-3 game-test gating, auditor blind-compare definition fix, reuse-scout verdict gate, doc line-number drift, console-capture-harness detail, engine-gotchas-vs-CLAUDE.md-not-duplicates). One domain item remains tracked in its OWN draft, not here: Weapon X villain per-card copy counts (needs a physical card count) — owned by docs/card-inventory/drafts/weapon-x.md. Inbox empty by design; see git history for the captured set. -->
