# Revelations → master — Merge Reconciliation Checklist

Run at merge time, AFTER Paul's dual-mode playtest sign-off. Coordinator-side (master). The expansion implementation + all reviews are complete; this is the finish-line housekeeping.

## 0. Gate status (all must be green before merge)
- [x] In-tree independent review (validator + /code-review high + stale-ref sweep) — done, 3 follow-up fixes applied (4b3658b Zero, d0df3dd Locations+Hellcat).
- [x] Sandbox review + audit (rules rulings) — done 2026-06-04; NO ruling wrong, doc-calibration only. Files: `D:\Claude Code\sandbox\reviews\2026-06-04-revelations-rules-rulings-{review,audit}.md`.
- [ ] **Paul's dual-mode playtest** (golden + whatif) using `docs/playtest-watch.md` (12 items) — PENDING, the last gate.

## 1. Rules-notes reconciliation (`docs/rules-notes/`) — CONTENT-MERGE, do NOT let the branch overwrite

**UPDATED 2026-06-14 (coordinator diagnosis — supersedes the "truncated/rebuild" framing below).** master's `docs/rules-notes/` is UNTRACKED but HEALTHY and AUTHORITATIVE; the `revelations` branch TRACKS small, independently-evolved stubs the worker created without ever seeing master's untracked files (untracked files don't exist in a worktree checkout). Sizes/content as of today:
- `revelations.md` — master **27KB, 4 clean sections** (Korvac LIFECYCLE; Ultimate Nullifier vs Korvac; Korvac "counts as Villain" placement; Earthquake/Tsunami escaped-Henchmen-count) starting cleanly at line 3 (NOT truncated — appears already rebuilt by a prior session). Branch **2.4KB, 2 sections** (Earthquake-Henchmen Obs 1/2/7; Korvac Revealed instant-win).
- `open-rules-questions.md` — master **16KB (~5 open questions)**. Branch **1.4KB — only the Cable+Hellcat F-G6 question** (Obs 2 already removed there per 2ccc5f9).
- `core.md` — master ONLY (~4KB, holds the 2026-06-14 superpower-icon "N OTHERS" ruling + other core rulings). NOT on branch → no collision.

**MERGE MECHANIC + RESOLUTION:** master's three files are being COMMITTED to master as part of safe-prep (so they're durable + tracked), which turns the merge into explicit add/add conflicts on `revelations.md` and `open-rules-questions.md` (NOT a silent branch-overwrite, NOT the untracked-refuse trap). Resolve each by **keeping master's superset and folding in any branch-unique content**:
- `revelations.md`: take master's 4 sections; verify the branch's "Korvac Revealed instant-win" ruling is represented (likely covered by master's Korvac lifecycle / Nullifier-win-trigger entries — confirm, fold if missing); the Earthquake-Henchmen ruling exists in both (take master's).
- `open-rules-questions.md`: take master's ~5 questions; confirm the branch's Cable+Hellcat F-G6 entry is present in master's (fold if missing); ensure Obs 2 is absent (settled).
- `core.md`: no conflict — just ensure it's committed/tracked at merge.

**Trivial collisions (same commit):** `.claude/agents/rules-oracle.md` (1 reordered line — immaterial, take either) and `.claude/skills/game-test/SKILL.md` (byte-identical — auto-merges, no conflict). Both committed to master in the same safe-prep infra commit.

### Prior-session rebuild notes (retained — may still flag branch-unique items to fold; master's file now appears rebuilt so most of this is likely already done — VERIFY against master's current 4 sections, do not blindly re-derive):
- **LOST (prior-session, not in any current context) — re-derive via `rules-oracle`:** (a) "reveals their hand and KOs/discards a [non-grey] Hero" target-scope; (b) Ultimate Nullifier vs Locations/Henchmen "enemy" scope = the **GP-6b BROAD** ruling (its gist survives inside the Nullifier-vs-Korvac entry's "RE THE GP-6b PREMISE" para, but the standalone entry is gone); (c) Secret HYDRA Corruption / Open HYDRA Revolution uses the NORMAL S.H.I.E.L.D. Officer stack.
- **RECOVERABLE (re-derivable via rules-oracle, or reconstruct):** Second Chance interception; play-a-copy resolve-now; What If? special henchman + Mandarin's Rings; Nightbringer "whenever you defeat a Villain" trigger; the Korvac-win entry HEAD (only its CONFIDENCE tail survives).
- **INTACT (leave):** Korvac lifecycle entry; Ultimate Nullifier vs Korvac entry.
- Apply sandbox-review polish while rebuilding: **E1** reorder the Korvac-lifecycle proof to lead with the decisive fact (Side B's Twist line literally says "This Scheme Transforms" → forces the toggle); demote the "Twist 8 would never land" flourish. **E2** reorder the Nullifier-vs-Korvac distinctions to lead with #1 (source) + #3 (nature); demote (don't delete) #2 (timing).
- Korvac twist numbering CONFIRMED against inventory lines 684–691 (2026-06-04) — Saga single Twist + Revealed 2/4/6/8 + "This Scheme Transforms" both sides. Physical-card spot-check remains Paul's to do at playtest.

## 2. Fold into master CLAUDE.md (from catalog "fold into CLAUDE.md at merge" + coordinator notes)
- **Embedded-quote scheme-name gotcha:** transforming-scheme Side-B DB names can carry literal double-quotes (`"No More Mutants"`); match on a side-stable field (`scheme.endGame`), never `scheme.name`.
- **Recruit-Granting Function Pattern:** every recruit grant pairs `totalRecruitPoints` + `cumulativeRecruitPoints` (recruit feeds Final Showdown), reset endTurn — the recruit twin of the documented Attack-Granting pattern.
- **`usesRecruitToFight` has THREE affordability gates, not two (caught 2026-06-11 playtest, Obs 17):** the documented "Duplicate updateHighlights() Gotcha" covers the two `updateHighlights()` twins, but `showAttackButton()` is the REAL city fight gate and ALSO needs the recruit-only branch (`totalRecruitPoints >= cost`, reserve excluded, attack-only). Missing it on `showAttackButton` let Mister Hyde show a fight button on Attack and then charge Recruit into the negative. Keep all THREE in sync for any `usesRecruitToFight` villain. (Extend the existing "Duplicate updateHighlights()" gotcha in CLAUDE.md to name `showAttackButton` as the third gate.)
- **Stale-cache (extend existing note):** Chromium serves STALE `cardAbilities.js` AND `expansionRevelations.js` even on a new port/fresh process; verify served-build freshness markers + eval-install the real fns before trusting any /game-test result. For interactive playtests use F12 → Empty Cache and Hard Reload (plain Ctrl+Shift+R may not suffice).
- **Off-pipeline fightEffect dispatch:** any non-city-fight dispatch of a villain `fightEffect` must save/null/restore the global `currentVillainLocation` (deck-revealed villains have no city position; position-reading effects like Lizard/Whirlwind misfire on stale state). Henchman fightEffects don't read it (F-G3 confirmed clean).
- **Game-test harness:** game start needs `pointerdown` on `#begin-game` (not `click`) — equivalently, use a TRUSTED Playwright click via `page.locator('#begin-game').click()`, which generates the real `pointerdown` the handler listens for; a scripted `element.click()` fires only a click event (no pointerdown) and silently no-ops (confirmed again 2026-06-14 Batch C smoke). Curl-to-localhost is permission-denied AND a denied call cancels sibling tool calls in the same batch — use Playwright `fetch`, never batch server calls with file ops.
- **Game-test harness (additional, 2026-06-10 Revelations playtest):** (1) Python `http.server` is single-threaded → bursts of webp/m4a requests throw `ERR_CONNECTION_REFUSED`, breaking card ART in screenshots (counts/DOM/logic unaffected — serve-only); use a threaded server for clean shots. (2) Broken images collapse layout → DOM detection by bounding-box/geometry is unreliable; use STRUCTURAL selectors (`#player-hand-element.children.length`, `.cell-label` text, `.card-container[data-city-index]`, `.popup-card`). (3) `display:none` on a start popup does NOT drain the popup QUEUE — loops reusing the shared `.info-or-choice-popup` element later collide with un-dequeued start popups (looks like an "orphaned Master Strike" mid-loop); drain the start queue via REAL buttons (`#intro-popup-close-button`, then `#info-or-choice-popup-confirm` until none remain) before driving popup-heavy abilities. (4) `offsetParent===null` is a FALSE-NEGATIVE visibility test for `position:fixed` popups (`#defeat-popup`) — use `getComputedStyle(el).display`. (5) Live UI-refresh tests MUST go through the real play path (`selectedCards=[idx]` → `confirmActions()`, or `endTurn()`) — abilities rely on the single trailing `updateGameBoard()`; an isolated ability call leaves the DOM stale and falsely reads as a refresh bug. (6) Playwright page can close mid-session with the server still up — re-navigate + re-`resize(1920×1080)` recovers.

## 3. CLAUDE.md copy reconciliation
- Worktree CLAUDE.md is the live copy during the expansion; master copy was frozen. Reconcile at merge. The "Session Roles & Folder Discipline" section is canonical in master — preserve it. The worktree "Session Roles" bullet-style is by design — don't "fix" it.

## 4. Residue cleanup (untracked, do NOT commit; route to `D:\Claude Code\_to-delete\` or leave)
- `chemistro-after-begin.png`, `hom-confirm-check.png`, `houseofM-after-begin.png` (worktree root), `docs/playwright-runs/2026-06-03/`.
- **master-side untracked residue (do NOT commit; harmless, doesn't block merge — leave or route to `_to-delete` at merge):** `setup-screen.png` (stray screenshot at repo root), `.playwright-mcp/` (Playwright MCP runtime artifacts — candidate for `.gitignore`). Neither collides with the branch.

## 5. Service worker (verify — worker reported both done)
- `sw.js` `CACHE_NAME` bumped v3→v4 ✓; `expansionRevelations.js` added to `FILES_TO_CACHE` (line 642) ✓. Confirm at merge.

## 6. Merge + deploy
- Merge `revelations` → master. Push. Verify GitHub Pages (hard refresh / cache-bust). Confirm the SW cache bump took.

## 7. Open items carried forward (not merge-blockers)
- `docs/rules-notes/open-rules-questions.md` — 5 open calls (play-a-copy timing the big one; Paul wants an official-FAQ check). Stays as a live revisit doc.
- Deferred catalog items (each-other-player review, polish list, etc.) remain in `docs/audit-results/revelations-2026-05-28.md` deferred section.
- New "unimplemented-ability cleanup" — Ronin/Hellcat were stale-flagged (already built); only out-of-scope extra-turn (Dark Dimension + Time Gem) remains, honest-logged.
- **POST-MERGE overlay/layout UX pass (cosmetic only — mechanics all confirmed working; Paul deferred 2026-06-14):** consolidate three render/layout glitches into one focused pass, land entry in `docs/known-issues.md` at merge: (a) **Obs 3** — Klaw's captured Hero not shown in the city; (b) **Obs 6** — rescued Bystander icon stays on the space and is oversized after a successful rescue; (c) **Obs 18** — HYDRA Officer (Secret HYDRA Corruption) placed cut-off on the left — Paul's design idea: render Officers smaller, sitting between the Scheme and Mastermind, slightly overlapping. Same captured/rescued-token render family; do together for consistent rendering rather than piecemeal. NOT merge-blockers.
- **POST-MERGE base-engine fix (NOT Revelations — do on master AFTER this merge, independently, to avoid `cardAbilities.js` divergence):** `koBonuses()` (cardAbilities.js:14-16) reads `koPile[koPile.length-1]` with no empty-guard and is called at cardAbilities.js:8923 (inside the `FightKOHeroYouHave` "KO a hero from hand" confirm) BEFORE the KO'd card is pushed (~8925). So a "KO a hero from hand" effect on turn 1 (empty koPile) → `kodCard` undefined → throws in the setTimeout, aborting the KO and HANGING the popup. Also reads the WRONG card (previous top) when non-empty — matters only for the Infinity Gems attack-reset (line 16). Fix: guard `koPile.length`, then (after grepping ALL `koBonuses()` callers) reorder push-before-koBonuses so it reads the correct card. Full gate + verify both modes; reproduce the empty-koPile crash first. Found 2026-06-10 Hydra Traitor playtest (masked there — Side B is only reached via a transform twist that fills koPile first). Captured as a task chip (`task_bfdcd807`, ephemeral) — THIS is the durable record.
- **POST-MERGE base-engine hygiene (NOT Revelations — pre-existing on BOTH branches, identical):** `showHeroAbilityMayPopup()` (master `script.js:~15957`; revelations `~17171` — same function, line numbers shifted by Revelations additions above it) contains four leftover debug `console.log` calls that fire on every popup show: "Confirm button display:", "Confirm button text:", "Deny button display:", "Deny button text:". F12-only, zero player/gameplay impact — pure hygiene, violates the onscreenConsole-vs-console rule. Do post-merge on the unified `script.js` in one edit: delete the four lines, run the JS syntax-check hook. They're side-effect-free logging calls, so removal cannot change behavior. Flagged by the Batch B cold-read reviewer 2026-06-14 (ephemeral task chip) — THIS is the durable record.
