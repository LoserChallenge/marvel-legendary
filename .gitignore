# Backup archives — the zip is redundant since all files are tracked here
*.zip

# Windows system files
Thumbs.db
desktop.ini

# Git worktrees
.worktrees/

# Rules PDFs — large files, stored locally only
rules/

# Superpowers plugin cache
.superpowers/
