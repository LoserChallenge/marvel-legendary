# Backup archives — the zip is redundant since all files are tracked here
*.zip

# Windows system files
Thumbs.db
desktop.ini

# Git worktrees
.worktrees/

# Rules PDFs — large files, stored locally only
rules/

# Expansion staging area — card images and inventory PDFs, local only
expansions/

# Superpowers plugin cache
.superpowers/

# Playwright MCP scratch output (snapshots, screenshots, console logs from /game-test)
.playwright-mcp/

# Playwright verification screenshots — disposable, re-runnable binary artifacts
docs/playwright-runs/
